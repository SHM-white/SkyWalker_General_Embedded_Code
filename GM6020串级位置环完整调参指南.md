# GM6020 串级位置环完整调参指南

## 1. 这份指南解决什么问题

当前样例是 GM6020 电流模式下的“位置外环 → 速度给定斜坡 → 速度内环 → 电流命令”串级控制。现象是：

- 到位很慢；
- 速度环 `Kp` 再增大一点，电机在转动过程中就抖。

先给结论：**不要再用增大速度环 `Kp` 的办法解决到位慢。** 当前现象至少由两个不同问题叠加：

1. 速度反馈只有整数 RPM 分辨率。直驱 `gear_ratio = 1` 时，每跳 1 RPM 就是 `0.10472 rad/s`。速度 `Kp` 越大，这个量化台阶被放大的电流跳变越大，很容易出现匀速转动中的啸叫、周期抖动或正负电流来回切换。
2. 当前速度给定的上升、下降斜率都只有 `1.5 rad/s²`。从 `1.2 rad/s` 降到 0 最少需要 `0.8 s`。位置外环已经要求减速时，斜坡器还会长时间保留旧速度给定，表现为制动晚、越过目标再返回，最终到位时间反而很长。
3. 当前位置死区 `0.0012 rad` 实际只有 `0.0688°`、约 1.56 个编码器 tick，并非注释写的 `0.69°`、16 tick。这一精度要求已经接近反馈量化边界，特别容易在静摩擦附近反复启停。
4. 工作区中的电流软件限幅已从提交版本的 `0.1 A` 放大到 `1.0 A`，设备树限幅是 `1.5 A`。这不会让最高巡航速度自动变快，却会明显加重抖动时的冲击和风险。

因此推荐顺序固定为：

> 机械与方向检查 → 限流 → 单独调稳速度内环 → 修正速度给定整形 → 调位置 P → 定死区 → 最后才按需加积分。

不要同时改多个参数，否则曲线改善后无法知道是哪一个参数起作用。

---

## 2. 本指南基于的实际代码与假设

主要文件：

- `samples/motor/dji_position_control/src/main.cpp`：串级位置环及 VOFA 数据；
- `samples/motor/dji_position_control/app.overlay`：GM6020、CAN、减速比和设备级电流上限；
- `samples/motor/dji_speed_control/src/main.cpp`：可用于隔离调速度环；
- `lib/control/pid.c`、`include/control/pid.h`：PID、死区、条件积分抗饱和的真实语义；
- `lib/control/slew_rate_limiter.c`：速度给定斜坡的真实语义；
- `drivers/motor/dji/dji_motor.cpp`：编码器展开、RPM 换算和电流命令入口；
- `drivers/motor/dji/dji_profiles.cpp`：GM6020 协议电流满量程为 `3 A`。

明确假设：

- 电机是 GM6020，工作在已确认的电流环固件模式；
- 当前设备树为 `gear-ratio-num = 1`、`gear-ratio-den = 1`，即直驱；
- 控制周期名义值为 5 ms（200 Hz），实际 `dt` 由系统时间测量；
- 目标是相对初始位置运动，当前测试步长为 `2 rad`（约 `114.6°`）；
- 注释给出的空载起转电流约 `0.05 A` 尚未用本次硬件重新测量；
- “抖”具体频率、负载惯量、传动间隙和位置精度要求未知，所以本文给出安全起点和判据，而不把某一组参数冒充最终值。

若实际有减速箱，文中的输出轴分辨率、速度和惯量判断都要按真实减速比重新计算；第一步应先修正设备树减速比。

---

## 3. 控制链和每个参数真正影响什么

```text
position_target_rad
        │
        ▼
位置误差 e_pos = target - position
        │
        ▼
位置 PID（输出单位 rad/s）
  Kp_pos、Ki_pos、位置死区、速度上限
        │ requested velocity
        ▼
速度给定斜坡器（单位 rad/s²）
        │ velocity_reference_rad_s
        ▼
速度误差 e_vel = vel_ref - velocity
        │
        ▼
速度 PID（输出单位 A）
  Kp_vel、Ki_vel、电流限幅
        │ current_command_a
        ▼
GM6020 自带电流环 → 电机和机械负载
        │
        └── position_rad、velocity_rad_s 反馈
```

### 3.1 参数职责表

| 参数 | 单位 | 主要效果 | 不能用它解决的问题 |
|---|---:|---|---|
| `kVelocityAbsMaxRadS` | rad/s | 长距离运动的最高巡航速度 | 静摩擦、末端抖动 |
| 速度给定斜率 | rad/s² | 启动和制动快慢 | 稳态速度误差 |
| `kInnerKp` | A/(rad/s) | 速度误差出现时立即给多少电流 | 不能无限加；会放大 RPM 量化和机械共振 |
| `kInnerKi` | A/(rad/s·s) | 消除负载/摩擦导致的稳态速度误差 | 快速响应；过大会低频摆动 |
| `kPositionKp` | 1/s | 位置误差换成多大的速度要求 | 电机的电流能力和速度内环稳定性 |
| `kPositionKi` | 1/s² | 消除恒定位置偏差 | 通常不是串级位置环的第一选择，易造成慢回摆 |
| `kPositionDeadbandRad` | rad | 允许的最终位置带，阻止量化附近反复追逐 | 运动过程的跟随误差 |
| `kSoftwareCurrentAbsMaxA` | A | 最大加速/制动转矩和安全边界 | 最高速度；空载时加大它通常只会更危险 |

### 3.2 本项目 PID 的特殊语义

`control_pid_step()` 的积分状态保存的已经是 **I 项输出**，不是未经乘 `Ki` 的误差积分：

```text
integral_output += Ki × effective_error × dt
```

所以 `integral_min/max` 的单位与控制器输出相同：

- 位置环积分限幅单位是 `rad/s`；
- 速度环积分限幅单位是 `A`。

PID 有条件积分抗饱和：如果本次积分会把输出更深地推入同向饱和，本次积分不会写回。但它没有积分回算，也不会自动处理斜坡器造成的二次约束。

死区使用 `fabs(error) <= deadband`。进入死区后 P、I 增量均按零误差计算；位置样例还会在 PID 计算后清零位置积分状态。注意，本周期的 `position.output` 已经计算完成，所以旧 I 项可能仍影响进入死区的第一个周期。

### 3.3 反馈分辨率

GM6020 驱动使用：

```text
position_rad = total_ticks × 2π / 8192 / gear_ratio
velocity_rad_s = integer_rpm × 2π / 60 / gear_ratio
```

直驱时：

- 1 个位置 tick = `0.00076699 rad = 0.04395°`；
- 16 个 tick = `0.01227 rad = 0.703°`；
- 1 RPM 速度台阶 = `0.10472 rad/s`。

因此：

- `kPositionDeadbandRad = 0.0012` 只有 1.56 tick，不是 16 tick；
- `kInnerKp = 0.04` 时，每跳 1 RPM，P 项约跳 `4.19 mA`；
- `kInnerKp = 0.12` 时，每跳 1 RPM，P 项约跳 `12.57 mA`。

这正是“速度 Kp 只加一点，转动时就抖”的重要机制之一。

---

## 4. 当前参数审计

当前工作区相对 Git 提交版本已有人工改动。不要直接丢弃这些改动，先执行：

```bash
git diff -- samples/motor/dji_position_control/src/main.cpp
```

当前运行参数及风险如下：

| 项目 | 当前值 | 提交版本值 | 判断 |
|---|---:|---:|---|
| 目标步长 | `2.0 rad` | `1.0 rad` | 行程翻倍，不能直接横比到位时间 |
| 位置 `Kp` | `10` | `6` | 外环已经不算慢，且更晚退出速度饱和 |
| 位置 `Ki` | `8` | `8` | 在内环未稳定前不应开启 |
| 位置 I 限幅 | `3 rad/s` | `0.30 rad/s` | 大于外环最终输出上限 `1.2`，过大且容易留存强积分作用 |
| 位置死区 | `0.0012 rad` | `0.012 rad` | 小了 10 倍，且注释未同步 |
| 最大速度 | `1.2 rad/s` | `1.2 rad/s` | 2 rad 行程仅匀速理论下限就为 1.67 s |
| 软件电流限幅 | `1.0 A` | `0.1 A` | 放大 10 倍；不是提速旋钮 |
| 速度 `Kp` | `0.04` | `0.12` | 若略增即抖，说明稳定裕度已不足，不应再加 |
| 速度 `Ki` | `0.01` | `0` | 应先用 P-only 确认稳定边界 |
| 速度 I 限幅 | `0.1 A` | `0` | 可达到已知空载起转电流的约 2 倍 |
| 上升/下降斜率 | `1.5/1.5 rad/s²` | `2.5/15` | 从满速归零需 0.8 s，是到位慢的重要嫌疑 |
| 设备树电流限幅 | `1.5 A` | `1.5 A` | 比软件限幅大；调参期第二道防线过宽 |

代码注释也已经与数值不一致：

- 注释说软件限流 `0.1 A`，实际 `1.0 A`；
- 注释说速度 P 的每 RPM 电流台阶约 `12.6 mA`，实际当前 Kp 下约 `4.2 mA`；
- 注释说下降率足够满足 `Kp_pos × cruise`，实际 `1.5 < 10 × 1.2 = 12 rad/s²`；
- 注释说死区约 `0.69°`，实际约 `0.069°`。

调参时必须同步修正注释，避免以后按错误假设继续加参数。

---

## 5. 先按症状分类，不要看到“慢”就加 Kp

| 曲线/现象 | 首要嫌疑 | 应调参数 |
|---|---|---|
| 大部分行程都顶着同一个低速度 | 最高速度限制太低 | `kVelocityAbsMaxRadS` |
| 启动后很久才爬到最高速度 | 启动斜率太低 | 与运动方向对应的 slew rate |
| 接近目标时仍保持明显同向速度，越过后才反向 | 制动斜率太低、速度给定滞后 | slew rate；优先修正整形逻辑 |
| 接近目标越来越慢，停在死区外 | 静摩擦、速度 PI 稳态能力不足、死区过小 | 先调速度 `Ki`，再定死区 |
| 匀速段速度呈一格一格跳，电流同步快速正负摆 | 速度反馈量化被 `Kp` 放大 | 降低速度 `Kp`；必要时滤波/观测器 |
| 低频地越过目标、返回、再越过 | 外环过强、位置积分过大、制动太晚 | 清零位置 `Ki`，降位置 `Kp`，增大制动能力 |
| 只有换向或齿隙处“咔哒” | 机械间隙/静摩擦 | 不能只靠 PID；检查机构和前馈 |
| 电流持续顶住限幅 | 电流能力不足或指令过激 | 不要先加 Kp；查负载、限流和速度要求 |
| 正向好、反向差 | 斜坡器语义导致方向不对称，或机械偏载 | 分方向测；修正轨迹/斜坡算法 |

---

## 6. 安全起始参数：先建立可调基线

第一次重新上电前，建议把 `samples/motor/dji_position_control/src/main.cpp` 顶部常量手工改为下面的安全基线：

```cpp
constexpr float kTargetOffsetRad = 0.30f;

constexpr float kPositionKp = 3.0f;
constexpr float kPositionKi = 0.0f;
constexpr float kPositionIntegralMaxRadS = 0.0f;
constexpr float kPositionDeadbandRad = 0.012f;

constexpr float kVelocityAbsMaxRadS = 0.60f;
constexpr float kSoftwareCurrentAbsMaxA = 0.15f;

constexpr float kInnerKp = 0.02f;
constexpr float kInnerKi = 0.0f;
constexpr float kInnerIntegralMaxA = 0.0f;

constexpr float kVelocityRampRateRisingRadS2 = 4.0f;
constexpr float kVelocityRampRateFallingRadS2 = 4.0f;
```

并把 `samples/motor/dji_position_control/app.overlay` 中的调参期限流改成：

```dts
current-limit-ma = <200>;
```

这里软件限流 `0.15 A` 是正常命令边界，设备树 `0.20 A` 是防止应用层数值误改的第二道边界。确认机构需要更大电流后，才逐级提高；不要一开始保留 `1.0/1.5 A`。

这组数值是安全起点，不是最终性能参数。若带载静摩擦已经超过 `0.15 A`，电机会不起转；此时按实测起转电流增大限流，例如使用 `1.5 × 起转电流`，同时仍受电机、驱动、供电和机构安全边界约束。

---

## 7. 先补齐观测量

当前位置样例的 9 路 VOFA 数据不足以区分“外环请求慢”和“斜坡器跟不上”。建议把发送数组扩成 16 路。`vofa_send()` 支持的上限正好是 16 个 float。

在 `samples/motor/dji_position_control/src/main.cpp` 的遥测数组位置，用下面的字段顺序作为参考：

```cpp
const float feedback_current_a =
    (feedback.valid & skywalker::motor::FeedbackCurrent) != 0U
        ? feedback.current_a
        : 0.0f;

const float channels[16] = {
    target_rad,                              // 0 目标位置 rad
    feedback.position_rad,                   // 1 实际位置 rad
    output.position.error,                   // 2 位置误差 rad
    output.position.p,                       // 3 位置 P 输出 rad/s
    output.position.i,                       // 4 位置 I 输出 rad/s
    output.position.output,                  // 5 斜坡前速度请求 rad/s
    output.velocity_reference_rad_s,         // 6 斜坡后速度给定 rad/s
    feedback.velocity_rad_s,                 // 7 实际速度 rad/s
    output.velocity.error,                   // 8 速度误差 rad/s
    output.velocity.p,                       // 9 速度 P 输出 A
    output.velocity.i,                       // 10 速度 I 输出 A
    output.current_command_a,                // 11 命令电流 A
    feedback_current_a,                      // 12 反馈电流 A
    output.position.saturated ? 1.0f : 0.0f, // 13 外环饱和
    output.velocity.saturated ? 1.0f : 0.0f, // 14 内环饱和
    dt_s,                                    // 15 实际周期 s
};
vofa_send(&vofa, channels, 16);
```

VOFA+ 使用 JustFloat、小端 float、帧尾 `0x7F800000`。RM Type-C 板的 `usart6` 当前是 115200 baud；16 路每帧为 68 字节，200 Hz 理论上要 13600 byte/s，超过 115200 8N1 的约 11520 byte/s 有效载荷。虽然发送函数忙时会丢帧而不阻塞控制，但为了得到稳定遥测，建议：

```cpp
constexpr std::uint32_t kTelemetryPeriodCycles = 2U; // 100 Hz
```

每轮实验至少保存以下记录：

| 实验号 | 负载 | 方向 | Kp_vel | Ki_vel | Kp_pos | Ki_pos | vmax | slew | 限流 | 上升时间 | 超调 | 进入并保持误差带时间 | 峰值电流 | 是否抖 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 01 | 空载 | + | | | | | | | | | | | | |

---

## 8. 实机调参的完整顺序

### 第 0 步：机械、电气和符号检查

1. 抬起或脱开负载，确保转子运动不会伤人或撞限位。
2. 备好物理断电手段；不要只依赖串口或软件停机。
3. 检查 GM6020 当前固件确实启用了电流环。
4. 检查 `motor-id = 4` 是否与实物一致。
5. 检查 `gear-ratio = 1/1` 是否真实。错误减速比会同时破坏位置、速度和增益单位。
6. 用很小的正电流确认位置反馈的正方向也为正。若电流为正而位置持续减小，先统一电机命令或反馈符号，禁止靠负 Kp“凑方向”；本 PID 配置拒绝负增益。
7. 手动缓慢转轴，观察静止位置是否单调、速度反馈是否以约 `0.1047 rad/s` 的格点变化。
8. 检查实际 `dt_s`。名义是 0.005 s；若经常接近 0.020 s 上限、周期跳变明显或控制直接返回 `-ERANGE`，先解决调度/CAN/日志问题。

通过条件：小电流方向正确、反馈无跳圈、CAN 反馈年龄不超时、机械无明显卡滞。

### 第 1 步：单独调速度内环 P

速度环必须先于位置环稳定。推荐用 `samples/motor/dji_speed_control` 做隔离测试，但当前该样例不是安全调参基线：它请求 `18 rad/s`、软件/设备树限流均为 `3 A`，运行 300 s。使用前必须手工降到小速度、小电流和短时间。

建议临时改为：

```cpp
constexpr std::int64_t kRunDurationMs = 9000;
constexpr float kRequestedVelocityRadS = 0.30f;
constexpr float kRequestedVelocityAbsMaxRadS = 0.60f;
constexpr float kSoftwareCurrentAbsMaxA = 0.15f;
```

速度控制器先设：

```cpp
.kp = 0.01f,
.ki = 0.0f,
.kd = 0.0f,
.derivative_tau_s = 0.0f,
.integral_min = 0.0f,
.integral_max = 0.0f,
.deadband = 0.0f,
```

把速度请求改为正负平台，以区分方向差异：

```cpp
float requestedVelocityForTime(std::int64_t elapsed_ms)
{
    if (elapsed_ms < 1000) return 0.0f;
    if (elapsed_ms < 4000) return 0.30f;
    if (elapsed_ms < 5000) return 0.0f;
    if (elapsed_ms < 8000) return -0.30f;
    return 0.0f;
}
```

同时把速度样例 `app.overlay` 的设备树限流由 `3000` 降至 `200 mA`。

按以下序列一次只改 `Kp_vel`：

```text
0.010 → 0.015 → 0.022 → 0.033 → 0.047 → 0.068 ...
```

每档同时测试 `±0.3 rad/s`；稳定后再测试 `±0.6` 和目标工作速度。不要只测试单方向或只测试空载。

找到“刚开始出现持续等幅抖动/啸叫”的临界值 `Kp_crit` 后：

```text
最终 Kp_vel = 0.5～0.7 × Kp_crit
```

若当前 `0.04` 稳定、稍加就抖，可先取 `0.025～0.030`，而不是继续向上试。留出温度、供电、负载和安装刚度变化的稳定裕度。

本步骤判据：

- 平台段没有持续周期抖动；
- 电流不会高频正负切换；
- 正负方向行为接近；
- 速度阶跃可快速跟随，即便仍有稳定偏差也暂时接受；
- 内环不长时间饱和。

### 第 2 步：只在需要时加入速度 Ki

保持刚选定的 `Kp_vel`，从小到大加 `Ki_vel`：

```text
0.002 → 0.005 → 0.010 → 0.020 A/(rad/s·s)
```

积分限幅先取略大于实测维持匀速所需电流：

```text
kInnerIntegralMaxA ≈ 1.2～1.5 × 空载/带载稳态电流
```

若空载维持电流约 `0.05 A`，可从 `0.06 A` 开始，不必直接用 `0.1 A`。观察平台速度误差消除时间，直到满足要求即停止增加。

出现以下任一现象就回退 `Ki` 30%～50%：

- 速度以低频上下摆；
- 速度换向后积分长时间不改符号；
- 电流到平台末端仍贴着积分限幅；
- 回到零速度后仍明显拖着轴转。

速度 D 先保持 0。因为本项目 D 项基于测量变化率，整数 RPM 的跳变除以 5 ms 会形成约 `20.94 rad/s²` 的单格尖峰；未经充分低通的 D 会直接制造电流脉冲。只有确认存在机械共振且 P/PI 无法取得足够相位裕度时，才试很小的 D，并令 `derivative_tau_s` 明显大于一个控制周期，例如从 `0.02～0.05 s` 起步。

### 第 3 步：回到位置样例，先关闭位置积分

把第 1、2 步得到的 `Kp_vel`、`Ki_vel` 和 I 限幅复制回 `dji_position_control`。位置环暂时保持：

```cpp
constexpr float kPositionKi = 0.0f;
constexpr float kPositionIntegralMaxRadS = 0.0f;
```

串级位置控制通常先用“位置 P + 速度 PI”。速度内环已经负责摩擦和负载的稳态电流，位置积分不是第一必需项。

### 第 4 步：先修速度给定斜坡，再谈外环快慢

当前位置外环 `Kp_pos = 10`、最高速度 `1.2` 时，未饱和区的理想速度请求变化率量级是：

```text
|d(v_request)/dt| ≈ Kp_pos × |velocity| ≤ 10 × 1.2 = 12 rad/s²
```

当前斜坡限幅只有 `1.5 rad/s²`，远小于 12，必然跟不上外环减速要求。若要让斜坡器不显著拖延制动，可用：

```text
slew_brake ≥ 1.2～1.5 × Kp_pos × vmax
```

例如：

- `Kp_pos = 3`、`vmax = 0.6`：建议至少 `2.2～2.7 rad/s²`，安全基线用 `4`；
- `Kp_pos = 6`、`vmax = 1.2`：建议至少 `8.6～10.8 rad/s²`，可先用 `10`；
- `Kp_pos = 10`、`vmax = 1.2`：建议至少 `14.4～18 rad/s²`，不能继续用 `1.5`。

#### 当前 slew limiter 的方向陷阱

`rising_rate_per_s` 和 `falling_rate_per_s` 是按“数值增大/减小”选择，不是按“加速/制动”选择：

- 正向启动 `0 → +v` 用 rising，正向制动 `+v → 0` 用 falling；
- 反向启动 `0 → -v` 用 falling，反向制动 `-v → 0` 用 rising。

所以 `rising = 2.5, falling = 15` 会导致两个方向的启动和制动完全不对称。若暂时不改算法，又要求正反方向一致，两者应设成相同值。

更好的最终实现是使用“按速度绝对值判断加速/制动”的方向无关限制器。应放在 `calculatePositionCurrent()` 调用斜坡器的位置，伪代码如下：

```cpp
delta = requested_velocity - current_velocity_reference;
same_direction = requested_velocity * current_velocity_reference >= 0;
speeding_up = same_direction &&
              abs(requested_velocity) > abs(current_velocity_reference);
rate = speeding_up ? acceleration_limit : deceleration_limit;
applied_delta = clamp(delta, -rate * dt, rate * dt);
current_velocity_reference += applied_delta;
```

边界行为要明确：

- 换向时先按制动上限减到 0，再按加速上限进入反向；
- 进入位置死区时，速度给定应优先向 0 收敛；
- `dt <= 0`、非有限数或状态未初始化仍要返回错误并触发安全停机；
- 状态只在完整计算成功后提交，维持当前事务式更新语义。

### 第 5 步：调最高速度和启动加速度

判断“全程慢”时先看 VOFA：

- 若 `position.output` 和 `velocity_reference` 长时间都贴着 `±vmax`，是最高速度限制；
- 若 `position.output` 很快到 `vmax`，但 `velocity_reference` 斜着很久才到，是启动斜率限制；
- 若 `velocity_reference` 已到目标而实际速度追不上，是速度环/限流/负载问题。

调参顺序：

1. 在小行程、低限流下先验证稳定。
2. 每次把 `vmax` 增加 20%～30%，不要翻倍。
3. 每次把启动加速度增加 20%～30%，观察峰值电流和机械冲击。
4. 始终让制动能力满足上一节公式；不要为“柔和启动”同时牺牲制动。

2 rad 行程、`vmax = 1.2 rad/s` 时，仅匀速理论下限就是 `1.67 s`，还未计入加减速。若业务要求 1 s 内完成，光调 Kp 不可能实现，必须提高轨迹速度/加速度上限，并确认电流、机械和安全能力允许。

### 第 6 步：调位置 Kp

保持位置积分为 0，从安全基线开始：

```text
Kp_pos: 2 → 3 → 4.5 → 6 → 8 ... 1/s
```

每档都测试正、负相同行程。找到开始出现明显超调或一次以上回摆的值后，回退 20%～30%。同时重新按 `Kp_pos × vmax` 公式检查制动斜率是否足够，否则看到的“外环过冲”可能实际是斜坡滞后。

P-only 未饱和区可近似为：

```text
e(t) = e(0) × exp(-Kp_pos × t)
```

因此更大的位置 Kp 主要缩短末端指数收敛时间，但也会让外环保持 `vmax` 更久、更晚开始减速。到一定程度后，继续加只会减少稳定裕度，不会提高受 `vmax` 限制的巡航速度。

### 第 7 步：设置位置死区

先明确业务允许误差。常用换算：

```text
0.1° = 0.001745 rad ≈ 2.28 tick（直驱）
0.5° = 0.008727 rad ≈ 11.38 tick
0.7° = 0.012217 rad ≈ 15.93 tick
1.0° = 0.017453 rad ≈ 22.76 tick
```

若业务允许 `±1°`，建议先用 `0.012 rad`（约 `±0.69°`），确认能稳定停住后再逐步减小：

```text
0.012 → 0.009 → 0.006 → 0.004 rad
```

死区不能小于静止位置噪声和机械回差能够稳定支持的范围。每档至少重复 10 次正负到位；只要出现反复起转、停转，就回到上一档。

当前代码只有死区，没有进入/退出不同阈值的迟滞。若对死区边缘反复穿越敏感，推荐最终实现双阈值：

```text
进入到位状态：|error| <= enter_band
退出到位状态：|error| >= exit_band，且 exit_band > enter_band
```

例如业务允许 `±1°` 时，可从 `enter = 0.010 rad`、`exit = 0.014 rad` 起测。到位状态内将位置积分清零，并把速度请求明确设为 0。

### 第 8 步：最后才判断是否需要位置 Ki

满足下列条件时通常不需要位置积分：

- 速度 PI 已能克服摩擦和恒定负载；
- 位置能进入允许死区并保持；
- 不存在长期同方向的外力或重力矩导致偏差。

若确实有持续负载，且 P-only 总停在死区外，再从很小值开始：

```text
Ki_pos: 0.1 → 0.2 → 0.5 → 1.0 1/s²
```

位置 I 限幅只需提供“爬入死区”的低速请求，建议先用：

```text
PositionIntegralMax = 0.05～0.20 rad/s
```

不要继续使用 `3 rad/s`；位置环最终输出本身只有 `±1.2 rad/s`，过大的 I 上限没有必要。若加入位置 Ki 后出现越过目标再慢慢回来、到位时间反而增长或周期摆动，应立即减小或关闭位置 Ki。

### 第 9 步：带上真实负载复调

空载稳定不等于带载稳定。按 25%、50%、100% 负载逐级测试：

1. 保持已验证的电流限幅；若确实不足，以 20%～30% 为一级提高。
2. 优先补速度 `Ki` 或物理前馈，不先提高速度 `Kp`。
3. 每一级都测试正向、反向、冷机、热机和电源电压上下限。
4. 检查安装刚度、联轴器、齿隙和线缆拖拽是否改变共振点。

若负载含重力方向，优先考虑使用现有 `feedforward_pid` 的重力/静摩擦前馈，而不是把位置积分加得很大。前馈只补偿可预测负载，反馈环继续负责误差；前馈符号和单位必须通过小电流试验确认。

---

## 9. 针对当前症状的最短修正路线

若希望先做最少的 A/B 实验，按下面四轮执行，每轮只改列出的项。

### A：确认末端慢是否来自斜坡滞后

保持 `KInnerKp = 0.04` 不再增加，先改：

```cpp
kPositionKi = 0.0f;
kPositionIntegralMaxRadS = 0.0f;
kPositionDeadbandRad = 0.012f;
kVelocityRampRateRisingRadS2 = 12.0f;
kVelocityRampRateFallingRadS2 = 12.0f;
```

同时把测试步长降到 `0.3 rad`、软件限流降到 `0.15 A`。如果“越过目标后慢慢回来”显著改善，主因就是速度给定斜坡，而不是速度 Kp 太小。

### B：确认转动中抖是否来自速度 Kp 稳定裕度

保持其他项不变，把：

```text
KInnerKp: 0.04 → 0.03 → 0.025
```

若匀速抖随之消失，而速度有小的稳态误差，说明方向正确：用小 `Ki` 消除稳态误差，不要把 Kp 加回去。

### C：恢复速度环稳态能力

从下面组合开始：

```cpp
kInnerKp = 0.025f;       // 或实测 0.5～0.7 Kp_crit
kInnerKi = 0.005f;
kInnerIntegralMaxA = 0.06f;
```

观察 3 s 恒速平台。如果稳态误差消除太慢，先把 Ki 加到 `0.01`；若低频摆动，则退回。

### D：恢复性能

确认 A～C 均稳定后，再逐级恢复：

```text
目标步长：0.3 → 0.6 → 1.0 → 2.0 rad
最高速度：0.6 → 0.8 → 1.0 → 1.2 rad/s
位置 Kp：3 → 4.5 → 6 1/s
```

每次提高位置 Kp 或最高速度，都重新计算最低制动斜率。不要一次恢复到 `Kp_pos = 10`、`2 rad` 和 `1 A` 限流。

---

## 10. 曲线判读模板

### 情况一：斜坡器拖慢制动

```text
position.output       ───────╲____
velocity_reference   ────────────╲____
velocity                         ───╲___
position_error                    0 后变负
```

特征：通道 5 已快速下降，通道 6 仍保持较大同向值。措施：提高制动斜率或改成方向无关加减速器。

### 情况二：速度 Kp 放大量化

```text
velocity_reference   ─────────────────
velocity             _-_-_-_-_-_-_-_  （约按 1 RPM 台阶跳）
current_command      +-+-+-+-+-+-+-+-  （同步反相跳）
```

特征：位置还远离目标，匀速段就抖。措施：降低速度 Kp，保留稳定裕度，再小量加入 Ki；必要时增加速度估计滤波。

### 情况三：位置积分造成慢回摆

```text
position_error       穿过 0
position.i           仍保持原符号
velocity_reference   仍推向原方向
```

措施：先关位置 Ki；若业务必须用，减小 Ki 和 I 限幅，并在进入到位状态时让本周期输出也立即归零。

### 情况四：限流或负载不足

```text
velocity_reference   已到目标
velocity             明显追不上
current_command      长时间贴 ±limit
saturated            长时间为 1
```

措施：查机械卡滞、供电、负载和限流。只有确认硬件允许才提高电流上限；加 Kp 对已经饱和的输出没有帮助。

---

## 11. 接口、状态和错误行为（修改时不能破坏）

### PID

```c
int control_pid_validate(const control_pid_config *config);
int control_pid_reset(control_pid_state *state,
                      float current_measurement);
int control_pid_step(control_pid_state *state,
                     const control_pid_config *config,
                     const control_pid_input *input,
                     control_pid_result *result);
```

- 成功返回 0；空指针、NaN/Inf 或非法配置返回 `-EINVAL`；
- `dt_s` 超出 `[dt_min_s, dt_max_s]` 返回 `-ERANGE`；
- `reset()` 清积分并用当前测量初始化微分状态，防止首周期 D 冲击；
- `step()` 先在局部状态计算，全部成功后才写回，失败时不应污染旧状态；
- 不是线程安全对象；一个控制器状态只能由一个控制上下文更新。

### Slew rate limiter

```c
int control_slew_rate_validate(const control_slew_rate_config *config);
int control_slew_rate_reset(control_slew_rate_state *state,
                            float current_value);
int control_slew_rate_step(control_slew_rate_state *state,
                           const control_slew_rate_config *config,
                           float requested_value,
                           float dt_s,
                           float *limited_value,
                           float *limited_rate);
```

- `dt_s <= 0` 返回 `-ERANGE`；未 reset 就 step 返回 `-EACCES`；
- rising/falling 按数值增减选择，不代表机械加速/制动；
- 若手工替换为方向无关限制器，要保留初始化、有限数检查、错误返回和失败不提交状态的行为。

### 电机与总线

```cpp
int skywalker::motor::setCurrent(const device *dev, float current_a);
int skywalker::motor::readFeedback(const device *dev, Feedback &out);
State skywalker::motor::getState(const device *dev);
```

- 超出设备树电流限幅返回 `-ERANGE`；未就绪返回 `-EHOSTDOWN`；未 arm 返回 `-EACCES`；
- CAN RX 回调在中断/驱动回调上下文中用 spinlock 更新反馈；主循环读取快照；
- 主循环任何控制错误都会调用 `stopAfterFailure()`，总线尝试发送全零命令并进入安全/故障状态；
- 不要为了调参绕开新鲜度、arm、epoch、限流或自动清零逻辑。

---

## 12. 构建、烧录和每步自检

本指南没有替你运行构建或上电测试。每次手工修改后，在 west 工作区可使用独立构建目录，避免不同板卡产物混用。

RM Type-C：

```bash
west build -p -b rm_typec samples/motor/dji_speed_control \
  -d ../build/dji_speed_control_rm_typec
west flash -d ../build/dji_speed_control_rm_typec

west build -p -b rm_typec samples/motor/dji_position_control \
  -d ../build/dji_position_control_rm_typec
west flash -d ../build/dji_position_control_rm_typec
```

若实际使用 DM-MC02，请先确认样例选择的 VOFA UART 节点和物理接口与板卡设备树一致，再将板名换为 `dm_mc02`；不要照搬 `usart6`。

### 安全上电顺序

1. 断电状态检查接线、CAN_H/L、终端电阻、电机 ID 和机械活动范围。
2. 让机构悬空或脱载，设置低软件限流和低设备树限流。
3. 只给控制板上电，确认日志、VOFA 和 CAN 配置。
4. 准备好物理断电后，再给电机母线供电。
5. 观察首次反馈是否新鲜、位置是否稳定，再允许样例 arm。
6. 一旦发生持续抖动、撞限位、反馈跳变、异常升温或电流持续饱和，立即物理断电。
7. 每次只改一个参数，重新构建、烧录、保存曲线和参数表。

### 每轮最低验收指标

应根据业务给出明确数值；尚未给出时，可先用以下临时指标：

- 最终位置进入并连续保持在 `±1°`；
- 正负方向各连续 10 次均不发生持续振荡；
- 超调不超过行程的 5%，且不出现二次过零回摆；
- 匀速段没有可感知的连续机械抖动；
- `dt_s` 无明显周期尖峰，反馈不超时；
- 命令电流不长期饱和，电机/驱动温度处于硬件允许范围；
- 断开反馈或触发错误时能够发送零命令并停机。

---

## 13. 不应照搬的现有内容

- 不要直接照搬 `dji_speed_control` 当前的 `18 rad/s`、`3 A`、300 s 配置做初调。
- 不要把 `drivers/pid` 的设备型 PID 与本样例的 `lib/control/pid` 混用。
- 不要按过时注释把当前 `0.0012 rad` 当成 16 tick。
- 不要把 current limit 当成最高速度旋钮。
- 不要用负 PID 增益补偿方向错误。
- 不要在速度反馈未滤波的情况下直接加入较大 D。
- 不要只测试正方向；当前斜坡器的 rising/falling 语义天然可能造成方向不对称。
- 不要在 `Kp_vel` 临界抖动边缘工作；应保留至少 30% 左右的稳定裕度。

可直接复用的内容：

- `readFreshPositionFeedback()` 的能力位、有限数和反馈年龄检查；
- 控制器局部状态计算成功后再提交的写法；
- `Bus::arm()`、`setCurrent()`、`flush()`、失败 `stop()` 的生命周期；
- VOFA JustFloat 异步发送方式；
- PID 已有的输出限幅和条件积分抗饱和。

---

## 14. 最终参数记录模板

```text
硬件：GM6020 / 固件版本：________
控制板：________  供电：________ V
减速比：________  负载/惯量：________
控制周期平均/最小/最大：________ / ________ / ________ ms

位置允许误差：±________ rad（±________°）
目标行程：________ rad
目标到位时间：________ s
允许超调：________ %

Kp_pos = ________ 1/s
Ki_pos = ________ 1/s²
Imax_pos = ________ rad/s
deadband_pos = ________ rad
vmax = ________ rad/s
accel = ________ rad/s²
decel = ________ rad/s²

Kp_vel = ________ A/(rad/s)
Ki_vel = ________ A/(rad/s·s)
Imax_vel = ________ A
software_current_limit = ________ A
device_tree_current_limit = ________ A

正向：上升时间 ________ s，超调 ________%，稳定时间 ________ s
反向：上升时间 ________ s，超调 ________%，稳定时间 ________ s
峰值/稳态电流：________ / ________ A
冷机/热机结论：________
```

---

## 15. 最终检查清单

- [ ] 已确认电机、反馈和位置误差符号一致。
- [ ] 已确认真实减速比，不是默认照搬 `1/1`。
- [ ] 调参期软件限流和设备树限流都已降低。
- [ ] 已把 2 rad 大步长暂时缩小，再逐级恢复。
- [ ] 已用独立速度样例先调稳速度 P，再按需加速度 I。
- [ ] 速度 `Kp` 已从临界振荡值回退到 50%～70%。
- [ ] 没有用增大速度 `Kp` 解决轨迹速度或制动斜率问题。
- [ ] 位置积分在外环初调时为 0。
- [ ] 位置死区已按 tick、角度和业务允许误差计算。
- [ ] 已检查 `slew >= 1.2～1.5 × Kp_pos × vmax` 的制动需求。
- [ ] 已理解 rising/falling 是数值方向，不是加速/制动方向。
- [ ] 已记录斜坡前速度请求、斜坡后速度给定和实际速度。
- [ ] 已测试正向和反向各至少 10 次。
- [ ] 已在空载稳定后按 25%、50%、100% 逐级加负载。
- [ ] 已检查持续饱和、温升、周期抖动、反馈超时和机械撞限位。
- [ ] 已验证错误路径能清零电流并安全停机。
- [ ] 已把所有参数注释同步到真实数值和单位。

完成以上步骤后，才把测试步长、速度、加速度和电流限制提升到业务目标。若仍然是“匀速转动中抖”，下一步应采集速度/电流的振荡频率，判断是整数 RPM 量化、机械共振还是控制周期/反馈延迟；这时应考虑速度估计低通或观测器，而不是继续提高速度 `Kp`。
