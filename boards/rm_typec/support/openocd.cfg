# STM32F407 OpenOCD Configuration for RM Type-C Board
# 使用外接 Horco 无线 DAPLink。OpenOCD 会自动优先尝试 CMSIS-DAP v2
# bulk，并在需要时回退到 CMSIS-DAP v1 HID。
source [find interface/cmsis-dap.cfg]

transport select swd

source [find target/stm32f4x.cfg]

# 无线链路首次连接先使用保守速度，稳定后可逐步提高。
adapter speed 1000
