# 达妙电机接口实现指南（第一阶段：MIT 模式）

## 1. 结论先行

本仓库中达妙电机应当作为独立的 `skywalker::motor::dm` 驱动实现，不能把达妙协议塞进现有 `dji` 目录，也不能复用 DJI 的“四台电机命令拼成一帧”发送逻辑。

推荐第一阶段只完成一条可安全闭环验证的链路：

```text
应用 setMitCommand()/通用 setTorque()
                │
                ▼
         DM 电机设备（缓存语义命令）
                │  flush，逐电机一帧
                ▼
      DM Bus ── Zephyr CAN ── DM-J4310
                ▲                   │
                │ Master ID 反馈帧   │
                └───────────────────┘
```

第一阶段应包含：

- MIT 五参数命令：`p_des`、`v_des`、`kp`、`kd`、`t_ff`。
- 特殊命令：使能、失能、清错；“保存零点”只编码，不在样例中调用。
- 统一反馈：位置、速度、扭矩、MOS 温度、线圈温度、驱动状态。
- 接入现有通用接口：`setTorque()`、`readFeedback()`、`getState()`。
- 反馈超时、命令超时、软件扭矩限幅、故障锁存和失能停机。
- 多电机共用同一 Master ID 时，由一个 `dm::Bus` CAN 过滤器按 `D[0]` 的电机 ID 分发。

第一阶段不做：

- 不修改电机内部参数、控制模式、CAN ID、Master ID、PMAX/VMAX/TMAX；这些先用达妙调试助手设置并人工核对。
- 不实现 UART 调参/固件升级。
- 不把位置或速度命令硬塞进当前通用 `motor::Api`。当前公共 API 没有位置/速度命令槽，先保留厂商接口 `dm::setMitCommand()`。
- 不做位置-速度模式和纯速度模式的实机运动测试。协议编解码可先写好，等 MIT 链路验证通过后再开放。

## 2. 文档依据与必须核对的硬件参数

已检查仓库内两份资料：

- `motor_docs/DM-J4310-2EC_V1.1_User_Manual.pdf`
- `motor_docs/调试助手使用说明书（达妙驱动控制协议）V1.4.pdf`

协议事实如下：

| 项目                    | 文档定义                                   |
| ----------------------- | ------------------------------------------ |
| CAN 帧                  | 标准帧，固定 1 Mbps                        |
| MIT 控制帧 ID           | `CAN_ID`                                 |
| 位置-速度控制帧 ID      | `0x100 + CAN_ID`                         |
| 纯速度控制帧 ID         | `0x200 + CAN_ID`                         |
| 反馈帧 ID               | `Master ID`，默认值为 0                  |
| MIT 数据宽度            | 位置 16 bit；速度、Kp、Kd、扭矩各 12 bit   |
| Kp 范围                 | `[0, 500]`                               |
| Kd 范围                 | `[0, 5]`                                 |
| 使能数据                | `FF FF FF FF FF FF FF FC`                |
| 失能数据                | `FF FF FF FF FF FF FF FD`                |
| 保存零点数据            | `FF FF FF FF FF FF FF FE`                |
| 清错数据                | `FF FF FF FF FF FF FF FB`                |
| J4310-2EC V1.1 额定电压 | 24 V（另有明确标注的 48 V 型号，不能混用） |
| CAN Timeout 单位        | 一个计数周期为 50 us                       |

上板前必须在调试助手中实际读取并记录以下值，不能只采用手册示例：

```text
电机型号/电压版本：________________
控制模式：MIT
CAN_ID：0x____
Master ID：0x____
PMAX：________ rad
VMAX：________ rad/s
TMAX：________ Nm
CAN Timeout：________ × 50 us
电机过温阈值：________ ℃
固件版本：________________
```

手册截图中的 `PMAX=12.5`、`VMAX=45`、`TMAX=18` 是协议配置示例，不是所有 J4310 的安全默认值。J4310 手册列出的峰值扭矩是 7 Nm，也不代表驱动器当前 `TMAX` 一定被配置成 7。代码中的量程必须与调试助手读取值完全一致，否则所有命令和反馈都会按比例出错。

### 文档中的歧义

V1.4 文档把反馈 `D[0]` 定义为 `ID | ERR << 4`，这只给 ID 留下低 4 bit；正文却写“ID 取 CAN_ID 的低 8 位”。两者互相矛盾。第一版按实际位布局解析：

```cpp
motor_id = data[0] & 0x0F;
status   = (data[0] >> 4) & 0x0F;
```

因此设备树先限制 `motor-id` 为 1～15。若实机确实需要大于 15 的 CAN ID，必须先抓包确认固件如何编码反馈，不能自行放宽。

“清除错误”表格漏写了 `D[2]` 表头，但数据含义显然是前 7 字节均为 `0xFF`，最后一个字节为 `0xFB`。仍需用调试助手或 USB-CAN 抓包做一次交叉确认。

## 3. 与现有代码的复用边界

### 可以复用

- `include/drivers/motor/motor.hpp` 中的 `State`、`Feedback`、`Api` 和内联调用器。
- `drivers/motor/dji/dji_motor.cpp` 的设备树实例化、`k_spinlock` 保护、反馈新鲜度判断、命令时间戳/epoch 思路。
- `drivers/motor/dji/dji_bus.cpp` 的 `init → attach → arm → flush → stop → recover` 生命周期和失败即停机原则。
- `samples/motor/dji_unified` 的设备获取、等待反馈、错误路径集中停机和低频打印结构。

### 不可照搬

- DJI 的 `command_id + command_slot` 分组。DM 是每台电机单独发送一帧，没有四电机槽位。
- DJI 的电流原始值换算。DM MIT 直接使用协议配置的扭矩范围 `TMAX`。
- DJI 的 8192 编码器展开和减速比再换算。DM 反馈已经按 PMAX 映射为 rad，且电机内部“减速比”参数会影响输出位置/速度；第一版不要再除一次减速比。
- DJI 的“全零 CAN 数据即零输出”。DM MIT 的全零数据代表所有字段的最小值，是危险命令；语义零命令必须经过映射编码。

默认示例量程下，语义命令 `{p=0, v=0, kp=0, kd=0, t=0}` 应近似编码为：

```text
7F FF 7F F0 00 00 07 FF
```

绝不能用 `00 00 00 00 00 00 00 00` 代替。

## 4. 文件级修改顺序

按下面顺序手工实现，每一步完成后先做本节自检，再进入下一步。

### 步骤 1：纯协议层

新增：

```text
include/drivers/motor/dm_protocol.hpp
drivers/motor/dm/dm_protocol.cpp
```

建议公开接口：

```cpp
namespace skywalker::motor::dm {

enum class ControlMode : std::uint8_t {
    Mit = 0,
    PositionVelocity,
    Velocity,
};

enum class DriveStatus : std::uint8_t {
    Disabled = 0x0,
    Enabled = 0x1,
    OverVoltage = 0x8,
    UnderVoltage = 0x9,
    OverCurrent = 0xA,
    MosOverTemperature = 0xB,
    MotorOverTemperature = 0xC,
    CommunicationLost = 0xD,
    Overload = 0xE,
    Unknown = 0xF,
};

enum class SpecialCommand : std::uint8_t {
    ClearError = 0xFB,
    Enable = 0xFC,
    Disable = 0xFD,
    SaveZero = 0xFE,
};

struct Limits {
    float position_max_rad;
    float velocity_max_rad_s;
    float torque_max_nm;
};

struct MitCommand {
    float position_rad = 0.0f;
    float velocity_rad_s = 0.0f;
    float kp = 0.0f;
    float kd = 0.0f;
    float torque_ff_nm = 0.0f;
};

struct RawFeedback {
    std::uint8_t motor_id = 0;
    DriveStatus status = DriveStatus::Unknown;
    std::uint16_t position_raw = 0;
    std::uint16_t velocity_raw = 0;
    std::uint16_t torque_raw = 0;
    std::uint8_t mos_temperature_c = 0;
    std::uint8_t rotor_temperature_c = 0;
    std::uint64_t timestamp_ms = 0;
};

struct DecodedFeedback {
    RawFeedback raw{};
    float position_rad = 0.0f;
    float velocity_rad_s = 0.0f;
    float torque_nm = 0.0f;
};

int controlFrameId(ControlMode mode,
                   std::uint16_t motor_id,
                   std::uint16_t &out);
int buildMitFrame(std::uint16_t motor_id,
                  const Limits &limits,
                  const MitCommand &command,
                  struct can_frame &out);
int buildPositionVelocityFrame(std::uint16_t motor_id,
                               float position_rad,
                               float velocity_rad_s,
                               struct can_frame &out);
int buildVelocityFrame(std::uint16_t motor_id,
                       float velocity_rad_s,
                       struct can_frame &out);
int buildSpecialFrame(ControlMode mode,
                      std::uint16_t motor_id,
                      SpecialCommand command,
                      struct can_frame &out);
int decodeFeedback(const struct can_frame &frame,
                   std::uint8_t expected_motor_id,
                   const Limits &limits,
                   DecodedFeedback &out);

} // namespace skywalker::motor::dm
```

#### 参数、返回值和边界

- 所有成功函数返回 0。
- 空指针不出现在上述引用式接口中；内部辅助函数遇到非法参数返回 `-EINVAL`。
- NaN、正负无穷、非正的 PMAX/VMAX/TMAX 返回 `-EINVAL`。
- 命令超出配置量程返回 `-ERANGE`，不要静默钳位。上层能据此发现单位或参数错误。
- `motor_id` 不在 1～15，或解析后的控制 ID 超过标准帧上限 `0x7FF`，返回 `-ERANGE`。
- 非 8 字节反馈、扩展帧、远程帧、CAN-FD 帧返回 `-EBADMSG`。
- 反馈 `D[0]` 的低 4 bit 与 `expected_motor_id` 不符，返回 `-ENOENT`，让总线分发器继续检查其他电机。
- 未知状态码仍解析原始数据，但上层必须将设备状态置为 `Fault`。

#### 定点映射

文档的编码公式等价于：

```cpp
int floatToUint(float x, float min, float max,
                unsigned bits, std::uint32_t &out)
{
    if (!std::isfinite(x) || !std::isfinite(min) ||
        !std::isfinite(max) || min >= max ||
        (bits != 12u && bits != 16u)) {
        return -EINVAL;
    }
    if (x < min || x > max) return -ERANGE;

    const std::uint32_t full_scale = (1u << bits) - 1u;
    const float scaled = (x - min) *
                         static_cast<float>(full_scale) /
                         (max - min);
    out = static_cast<std::uint32_t>(scaled); // 与手册示例一致：截断
    if (out > full_scale) out = full_scale;   // 只修正浮点端点误差
    return 0;
}

float uintToFloat(std::uint32_t x, float min, float max,
                  unsigned bits)
{
    const std::uint32_t full_scale = (1u << bits) - 1u;
    return static_cast<float>(x) * (max - min) /
           static_cast<float>(full_scale) + min;
}
```

位置、速度、扭矩分别使用 `[-PMAX,+PMAX]`、`[-VMAX,+VMAX]`、`[-TMAX,+TMAX]`。Kp 使用 `[0,500]`，Kd 使用 `[0,5]`。

MIT 打包必须逐项写清楚，不用 C/C++ 位域：

```cpp
out.data[0] = static_cast<std::uint8_t>(p >> 8);
out.data[1] = static_cast<std::uint8_t>(p);
out.data[2] = static_cast<std::uint8_t>(v >> 4);
out.data[3] = static_cast<std::uint8_t>(((v & 0x0Fu) << 4) |
                                        (kp >> 8));
out.data[4] = static_cast<std::uint8_t>(kp);
out.data[5] = static_cast<std::uint8_t>(kd >> 4);
out.data[6] = static_cast<std::uint8_t>(((kd & 0x0Fu) << 4) |
                                        (torque >> 8));
out.data[7] = static_cast<std::uint8_t>(torque);
```

反馈解包：

```cpp
const std::uint8_t status = frame.data[0] >> 4;
const std::uint8_t id = frame.data[0] & 0x0F;
const std::uint16_t p =
    (static_cast<std::uint16_t>(frame.data[1]) << 8) |
    frame.data[2];
const std::uint16_t v =
    (static_cast<std::uint16_t>(frame.data[3]) << 4) |
    (frame.data[4] >> 4);
const std::uint16_t t =
    (static_cast<std::uint16_t>(frame.data[4] & 0x0F) << 8) |
    frame.data[5];
```

位置-速度和纯速度模式传的是 IEEE-754 `float` 小端字节。不要用指针强转造成严格别名和端序问题；项目是 C++20，可使用 `std::bit_cast<std::uint32_t>(value)` 后显式取低字节。位置-速度帧 DLC 为 8，纯速度帧 DLC 为 4。

#### 步骤 1 自检

至少手算并断言这些向量：

1. 示例量程 `±12.5/±45/±18` 下的语义零 MIT 帧是 `7F FF 7F F0 00 00 07 FF`。
2. 使能、失能、保存零点、清错分别以 `FC/FD/FE/FB` 结尾，前 7 字节全为 `FF`。
3. `D3=0xC7`、`D4` 高半字节为 `1` 时，速度原始值是 `0xC71=3185`；VMAX=45 时解码应约为 25 rad/s。
4. `1.0f` 的小端字节为 `00 00 80 3F`，`-1.0f` 为 `00 00 80 BF`。
5. NaN、越界值、错误 DLC、扩展帧、RTR 帧、FDF 帧、错误电机 ID 均返回错误。

### 步骤 2：DM 电机设备

新增：

```text
include/drivers/motor/dm_motor.hpp
drivers/motor/dm/dm_internal.hpp
drivers/motor/dm/dm_motor.cpp
drivers/motor/dm/dm_j4310_instance.cpp
```

`dm_motor.hpp` 建议公开：

```cpp
struct Descriptor {
    const struct device *can = nullptr;
    ControlMode mode = ControlMode::Mit;
    std::uint16_t motor_id = 0;
    std::uint16_t master_id = 0;
    std::uint16_t control_id = 0;
    Limits limits{};
    float torque_limit_nm = 0.0f;
};

int describe(const struct device *dev, Descriptor &out);
int setMitCommand(const struct device *dev,
                  const MitCommand &command);
int readRawFeedback(const struct device *dev,
                    RawFeedback &out);
int getDriveStatus(const struct device *dev,
                   DriveStatus &out);
```

内部配置建议为：

```cpp
struct DmConfig {
    const struct device *can;
    std::uint32_t motor_id;
    std::uint32_t master_id;
    ControlMode mode;
    std::uint32_t p_max_millirad;
    std::uint32_t v_max_millirad_s;
    std::uint32_t t_max_millinewton_meter;
    std::uint32_t torque_limit_millinewton_meter;
};
```

内部运行数据至少包含：

```cpp
struct DmData {
    Feedback feedback{};
    RawFeedback raw_feedback{};
    DriveStatus drive_status = DriveStatus::Unknown;
    MitCommand staged_command{};
    std::uint64_t last_rx_ms = 0;
    std::uint64_t command_stamp_ms = 0;
    std::uint64_t active_epoch = 0;
    std::uint64_t command_epoch = 0;
    bool command_valid = false;
    bool armed = false;
    bool fault_latched = false;
    struct k_spinlock lock{};
};
```

初始化时完成这些校验：

- CAN 设备非空且 ready。
- `motor-id` 为 1～15；`master-id` 不超过 `0x7FF`。
- 三个量程都大于 0，软件扭矩限幅大于 0 且不超过 TMAX。
- 第一阶段只允许 `ControlMode::Mit`；若设备树选其他模式，返回 `-ENOTSUP`，避免“编译成功但协议模式不一致”。
- `0x000～0x7FF` 范围内的最终控制 ID 合法。

`skywalker::motor::Api` 的第一版映射为：

```cpp
const skywalker::motor::Api dm_motor_api = {
    getCapabilities,
    nullptr,          // set_current：DM 反馈/命令是输出扭矩，不是假定相电流
    setTorqueImpl,
    readFeedbackImpl,
    getStateImpl,
};
```

能力位为：

```cpp
CommandTorque |
FeedbackPosition |
FeedbackVelocity |
FeedbackTorque |
FeedbackTemperature
```

公共 `Feedback::temperature_c` 使用线圈/转子温度，因为它更接近电机热安全边界；MOS 温度仍保留在 `RawFeedback::mos_temperature_c` 中。不要声明 `FeedbackCurrent`，文档反馈的是扭矩而不是相电流。

`setTorqueImpl(dev, torque_nm)` 的语义是纯 MIT 扭矩模式：

```cpp
MitCommand command{};
command.torque_ff_nm = torque_nm;
return setMitCommand(dev, command);
```

这会把 p/v/kp/kd 都置零，而不是在上一条混合命令上只改 `t_ff`。此语义必须写进头文件注释，防止调用者误以为它会保留上次 Kp/Kd。

`setMitCommand()` 只验证并缓存命令，不直接调用 `can_send()`：

- 所有字段必须有限。
- p/v/kp/kd/t 必须在协议量程内。
- `abs(torque_ff_nm)` 还必须不超过软件 `torque_limit_nm`。
- 设备未 armed 返回 `-EACCES`。
- 反馈超时或状态为故障返回 `-EHOSTDOWN`。
- 成功时在自旋锁内更新命令、时间戳、epoch、valid 标记。

状态映射：

| 条件                                    | 通用`State` |
| --------------------------------------- | ------------- |
| 从未收到反馈或反馈超时                  | `Offline`   |
| 收到 0（失能）或 1（使能），且反馈新鲜  | `Ready`     |
| 收到 8～E、未知状态码或本地发送故障锁存 | `Fault`     |

通用 `State::Ready` 只代表通信和反馈正常，不等于驱动已使能。是否使能必须通过 `getDriveStatus()` 判断。

反馈接收函数由 Bus 分发调用，建议做成 `dm_internal.hpp` 中的：

```cpp
int acceptFeedback(const struct device *dev,
                   const struct can_frame &frame,
                   std::uint64_t now_ms);
```

它在锁外做格式和定点转换，在锁内一次性提交 `raw_feedback`、`feedback`、`drive_status` 和 `last_rx_ms`。错误状态必须置 `fault_latched=true`；普通 0/1 状态不能自动清掉已经锁存的本地发送故障，清错只能走明确的 recover 流程。

#### 步骤 2 自检

- `capabilities()` 不含 `CommandCurrent` 和 `FeedbackCurrent`。
- `setCurrent()` 返回 `-ENOTSUP`。
- `setTorque()` 在未 arm 时返回 `-EACCES`，越过软件扭矩限幅返回 `-ERANGE`。
- 反馈状态 0 与 1 都映射为通用 `Ready`，但 `getDriveStatus()` 能区分两者。
- 状态 8～E 和未知状态都进入 `Fault`。
- 公共温度取 rotor，原始接口可同时读 MOS 与 rotor。

### 步骤 3：DM Bus 与安全生命周期

新增：

```text
include/drivers/motor/dm_bus.hpp
drivers/motor/dm/dm_bus.cpp
```

外部接口保持与 DJI Bus 相似，方便应用迁移，但报告字段改成逐电机帧：

```cpp
enum class BusState : std::uint8_t {
    Uninitialized = 0,
    Safe,
    Armed,
    Fault,
};

struct TxReport {
    int preparation_error = 0;
    int tx_error = 0;
    std::uint16_t failed_motor_id = 0;
    std::uint8_t frames_expected = 0;
    std::uint8_t frames_sent = 0;
};

class Bus {
public:
    int init(const struct device *can);
    int attach(const struct device *motor);
    int arm(TxReport &report);
    int flush(TxReport &report);
    int stop(TxReport &report);
    int recover(TxReport &report);
    BusState state() const;
};
```

#### 接收分发

同一条 CAN 总线上的多个达妙电机通常可以使用相同 Master ID。因此 Bus 对每个不同的 Master ID 只注册一个 `can_add_rx_filter()`：

```text
CAN frame.id == master_id
        │
        ▼
读取 D[0] 低 4 bit motor_id
        │
        ├─ 匹配 (master_id, motor_id) → acceptFeedback(motor A)
        ├─ 匹配 (master_id, motor_id) → acceptFeedback(motor B)
        └─ 无匹配 → 忽略并计数（不要在回调里刷日志）
```

`attach()` 必须拒绝：

- 不是 DM 设备：`-ENOTSUP`。
- 电机引用了其他 CAN 控制器：`-EXDEV`。
- 同一设备重复 attach：`-EALREADY`。
- `(master_id, motor_id)` 重复：`-EADDRINUSE`。
- 最终控制帧 ID 重复。例如两台 MIT 电机虽然使用不同 Master ID，却都配置为 `CAN_ID=1`，它们的控制帧都会使用 `0x001`；即使反馈路由不冲突，命令仍会同时控制两台电机，必须拒绝。
- 超过静态数组容量：`-ENOSPC`。

在数组中先保留槽位、再添加 CAN filter 时要考虑回调可能立刻发生；若添加 filter 失败，要回滚刚加入的槽位。Bus 和电机对象都应为静态生命周期，不支持运行时析构。

#### 发送和状态机

`init(can)`：校验设备、接受 `can_start()` 返回 `-EALREADY`、进入 `Safe`。不要复制 DJI 私有的 owner registry 来禁止 DM 与其他协议共享物理 CAN；CAN 本身允许多个协议共线。但同一个 DM 电机只能归属一个 DM Bus。

`arm(report)`：

1. 只允许从 `Safe` 进入。
2. 所有电机必须有新鲜反馈、无故障，并且反馈状态为 `Disabled`；状态为 `Enabled` 时返回 `-EBUSY`，先执行 `stop()` 并等到失能反馈。
3. 为每台 MIT 电机写入“语义零命令”缓存，绝不能缓存全零线帧。
4. 增加非零 lifecycle epoch。
5. 逐台发送使能特殊帧；任一发送失败，立刻对所有已 attach 电机尝试发送失能，锁存 `Fault`。
6. 全部成功后才把各电机本地状态置 armed，Bus 进入 `Armed`。

`flush(report)`：

1. 只允许 `Armed`。
2. 在发任何一帧前，先快照所有电机命令；任一命令过期、反馈过期、epoch 不匹配或电机故障，整批不发送，转故障失能。
3. 每台电机各构造一帧并 `can_send(..., K_MSEC(2), ...)`。
4. 任一发送失败，停止后续运动命令，对所有电机尽力发送失能，Bus 进入 `Fault`。

`stop(report)`：

1. 先在锁内把所有电机 `armed=false`，使并发 `setMitCommand()` 立即失败。
2. 逐台发送失能特殊帧，不发送“位置=0”的命令。
3. 全部发送成功时进入 `Safe`；发送失败时进入 `Fault`。

`recover(report)`：

1. 仅允许从 `Fault` 调用。
2. 对每台电机发送清错，再发送失能。
3. 发送完成不等于故障已经消失。保持 `Fault`，直到收到每台电机新鲜的 `Disabled` 反馈，且没有 8～E 错误后，再由第二次 `recover()` 或单独的 `pollRecovery()` 切到 `Safe`。

不要在 `recover()` 内无限睡眠等待反馈。库接口保持非阻塞，超时和重试由应用控制。

#### 并发语义

- CAN RX 回调按“不可睡眠上下文”处理：只解析、短时间持有 `k_spinlock`、更新内存；不能 `k_sleep()`、不能 `can_send()`、不能大量日志。
- `setMitCommand()` 与 `readFeedback()` 可从普通线程并发调用，靠每电机自旋锁保护。
- `Bus::init/attach/arm/flush/stop/recover` 只允许一个控制线程调用；Bus 容器本身不为多调用者加锁。
- `can_send()` 带等待时间，只能从线程上下文调用。
- 不在锁内调用 `can_send()`，避免长时间关抢占/关中断。

#### 步骤 3 自检

- 两台相同 Master ID、不同 motor ID 的反馈能各自更新正确设备。
- 相同 `(Master ID, motor ID)` 会被拒绝。
- arm 前收到“使能”状态时不会直接继续，而是要求先失能。
- 一台命令过期会让整条 DM Bus 失能，而不是继续发送其他电机命令。
- 停机发送的是特殊失能帧 `... FD`。
- 清错后未收到正常失能反馈时仍保持 Fault。

### 步骤 4：设备树、Kconfig 和构建接线

新增：

```text
dts/bindings/motor/dm-motor-base.yaml
dts/bindings/motor/dm,j4310-2ec-v1-1.yaml
drivers/motor/dm/CMakeLists.txt
```

修改：

```text
drivers/motor/CMakeLists.txt
drivers/motor/Kconfig
```

`dm-motor-base.yaml` 建议属性：

```yaml
description: Common properties for a Damiao CAN motor
include: base.yaml

properties:
  can-bus:
    type: phandle
    required: true
  motor-id:
    type: int
    required: true
    min: 1
    max: 15
  master-id:
    type: int
    required: true
    min: 0
    max: 2047
  control-mode:
    type: string
    required: true
    enum: ["mit", "position-velocity", "velocity"]
  p-max-millirad:
    type: int
    required: true
    min: 1
  v-max-millirad-s:
    type: int
    required: true
    min: 1
  t-max-millinewton-meter:
    type: int
    required: true
    min: 1
  torque-limit-millinewton-meter:
    type: int
    required: true
    min: 1
```

用整数毫单位是因为 Devicetree 属性不适合直接放 C/C++ `float`。实例宏中除以 1000.0f，且必须再次做运行时有限性和范围校验。

型号 binding：

```yaml
description: Damiao DM-J4310-2EC V1.1 integrated CAN motor
compatible: "dm,j4310-2ec-v1-1"
include: dm-motor-base.yaml
```

不要把 PMAX/VMAX/TMAX 写成型号 binding 的固定默认值，它们能被调试助手改变；强制每个实例显式填写可以避免固件配置和代码量程悄悄不一致。

Kconfig 建议：

```kconfig
config SKYWALKER_MOTOR_DM
    bool "Damiao CAN motor family"
    depends on CAN

if SKYWALKER_MOTOR_DM

config SKYWALKER_DM_FEEDBACK_TIMEOUT_MS
    int "Damiao feedback freshness timeout"
    default 50
    range 2 1000

config SKYWALKER_DM_COMMAND_TIMEOUT_MS
    int "Damiao command cache timeout"
    default 20
    range 1 1000

config SKYWALKER_DM_MAX_MOTORS_PER_BUS
    int "Maximum Damiao motors per bus"
    default 8
    range 1 15

config SKYWALKER_DM_MAX_MASTER_IDS_PER_BUS
    int "Maximum distinct Damiao Master IDs per bus"
    default 4
    range 1 15

endif
```

顶层电机 CMake 中把已有注释替换为：

```cmake
add_subdirectory_ifdef(CONFIG_SKYWALKER_MOTOR_DM dm)
```

DM 子目录编译：

```cmake
zephyr_library()
zephyr_library_sources(
    dm_protocol.cpp
    dm_motor.cpp
    dm_bus.cpp
    dm_j4310_instance.cpp
)
```

实例文件使用与 DJI 相同的 `DT_DRV_COMPAT` + `DT_INST_FOREACH_STATUS_OKAY()` 模式。`control-mode` 可通过 `DT_INST_ENUM_IDX(inst, control_mode)` 映射到枚举，但必须用 `static_assert` 或清晰的转换函数保证 binding 中 enum 顺序和 C++ 枚举顺序一致。

#### 步骤 4 自检

- 不启用 `CONFIG_SKYWALKER_MOTOR_DM` 时，新增源码不参与编译。
- 缺少任一量程属性时 Devicetree 构建阶段报错。
- `motor-id=16`、`master-id=2048` 在 Devicetree 校验阶段报错。
- `control-mode="velocity"` 在第一阶段设备初始化返回 `-ENOTSUP`，而不是误用 MIT 帧。

### 步骤 5：最小上板样例

新增：

```text
samples/motor/dm_mit_control/CMakeLists.txt
samples/motor/dm_mit_control/prj.conf
samples/motor/dm_mit_control/app.overlay
samples/motor/dm_mit_control/src/main.cpp
```

overlay 示例中的数值必须替换成调试助手实读值：

```dts
/ {
    aliases {
        motor0 = &dm_j4310_1;
    };

    dm_j4310_1: motor-1 {
        compatible = "dm,j4310-2ec-v1-1";
        status = "okay";
        can-bus = <&can1>;
        motor-id = <1>;
        master-id = <0>;
        control-mode = "mit";

        /* 以下仅为手册示例；上板前替换成调试助手实读值。 */
        p-max-millirad = <12500>;
        v-max-millirad-s = <45000>;
        t-max-millinewton-meter = <18000>;

        /* 第一次测试最多 0.05 Nm，并确保机构允许。 */
        torque-limit-millinewton-meter = <50>;
    };
};
```

`prj.conf` 至少包含：

```conf
CONFIG_CPP=y
CONFIG_REQUIRES_FULL_LIBCPP=y
CONFIG_CAN=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_MAIN_STACK_SIZE=8192
CONFIG_SKYWALKER_DRIVER_MOTOR=y
CONFIG_SKYWALKER_MOTOR_DM=y
CONFIG_SKYWALKER_DM_FEEDBACK_TIMEOUT_MS=50
CONFIG_SKYWALKER_DM_COMMAND_TIMEOUT_MS=20
```

样例执行顺序：

```cpp
const struct device *motor = DEVICE_DT_GET(DT_ALIAS(motor0));
dm::Descriptor descriptor{};
dm::Bus bus;

// 1. device_is_ready + describe + bus.init + bus.attach
// 2. 立即 bus.stop()，处理“MCU 重启但电机仍使能”的情况
// 3. 等待 fresh feedback 且 DriveStatus::Disabled
// 4. bus.arm()；等待反馈变为 DriveStatus::Enabled
// 5. 每 5 ms：setTorque(0.0f) + bus.flush()，先保持 2 秒
// 6. 确认位置/速度/温度方向和数值正常后，才短时给 0.02～0.05 Nm
// 7. 任一错误、超速、过温或测试结束：bus.stop()
```

不要一启动就给 1 Nm。手册明确提示：MIT 纯扭矩模式空载时，即使很小扭矩也会持续加速到最高转速。

建议样例保护阈值：

- 主循环周期 5 ms；每圈都刷新命令。
- 测试扭矩从 0 开始，人工确认后使用 0.02 Nm，再到 0.05 Nm。
- 第一次只持续 0.5～1 秒有扭矩输出。
- 输出轴速度绝对值超过 1 rad/s 立即失能（根据机构再调整）。
- 线圈或 MOS 温度超过 60 ℃立即失能。
- 反馈超过 50 ms、命令超过 20 ms、任何状态 8～E，立即失能。
- 日志每 100～200 ms 打印一次，不能每个 CAN 回调打印。

## 5. 推荐的测试文件与测试向量

协议层是纯函数，建议新增 Zephyr ztest：

```text
tests/drivers/motor/dm_protocol/
├── CMakeLists.txt
├── prj.conf
├── testcase.yaml
└── src/main.cpp
```

测试组：

| 测试         | 预期                                  |
| ------------ | ------------------------------------- |
| MIT 语义零   | 精确等于`7F FF 7F F0 00 00 07 FF`   |
| MIT 最小边界 | P/V/Kp/Kd/T 原始值全 0                |
| MIT 最大边界 | 16 bit 为`FFFF`，12 bit 为 `FFF`  |
| 手册速度示例 | `0xC71`、VMAX=45 解码约 25 rad/s    |
| 特殊帧       | 前 7 字节 FF，末字节逐项正确          |
| PV 小端      | `p=1.0f` → `00 00 80 3F`         |
| 速度小端     | `v=-1.0f` → `00 00 80 BF`，DLC=4 |
| 反馈状态     | 0、1、8～E 分别正确映射               |
| 非法数值     | NaN/Inf/越界均失败且不改写输出对象    |
| 非法 CAN 帧  | IDE/RTR/FDF/错误 DLC/错误 ID 均失败   |

所有 builder/decoder 都采用“先构造局部 `next`，全部成功后再赋给 out”的提交方式。错误返回时输出参数保持原值，便于调用方判断。

## 6. 建议构建命令（由你实现后手工执行）

根据仓库的古法编程约束，本指南没有替你运行会产生 `build/` 的命令。完成对应源码后，在仓库根目录执行：

```bash
# 先只构建协议测试
west twister -T tests/drivers/motor/dm_protocol -p native_sim

# 再编译上板样例
west build -p -b dm_mc02 samples/motor/dm_mit_control

# 人工复核 overlay 参数无误、机构悬空、急停可用后再烧录
west flash
```

如果当前 Zephyr 版本的 `native_sim` 不支持该 C++/CAN 头文件组合，可直接构建 `dm_mc02` 测试镜像，但不要为了测试协议纯函数而启动真实 CAN 发送。

## 7. 实机上电顺序

1. 电机与机构脱开，或把机构可靠架空并限位；准备能立刻切断电机母线的急停。
2. 核对电机铭牌是 24 V 还是 48 V 版本。J4310-2EC V1.1 普通版文档要求 24 V。
3. 断电状态连接 CAN_H、CAN_L、GND；确认总线两端终端电阻，断电测 CAN_H/CAN_L 约 60 Ω。
4. 先只给 MC02 控制板供电，确认固件启动。
5. 给电机上电，观察红灯常亮（失能）并等待反馈；此时不发送运动命令。
6. 日志确认 CAN ID、Master ID、状态 0、位置/速度/温度合理。
7. 调用 `arm()`，确认绿灯和状态 1；持续发送语义零命令。
8. 先用手缓慢转动输出轴，确认位置/速度方向和量纲。
9. 仅在确认保护路径有效后，短时给 0.02～0.05 Nm；达到时间或速度阈值立即失能。
10. 结束时软件失能，再切断电机母线。不要靠控制板复位代替电机失能。

## 8. 故障排查

| 现象               | 优先检查                                                                     |
| ------------------ | ---------------------------------------------------------------------------- |
| 完全无反馈         | CAN 1 Mbps、H/L 是否反接、共地、终端电阻、Master ID 过滤器、电机供电         |
| 收到帧但实例不更新 | `frame.id` 是否等于 Master ID；`D[0]&0x0F` 是否等于 motor-id             |
| 反馈比例明显不对   | PMAX/VMAX/TMAX 是否与调试助手当前值完全一致                                  |
| 一使能就猛转       | 是否误发了全零数据；是否 p/v/kp/kd/t 的映射或 nibble 拼接错误                |
| 纯扭矩空载持续加速 | 协议本来如此；减小扭矩、加速度保护并缩短测试时间                             |
| 状态 D（通讯丢失） | 主循环/flush 周期太慢，CAN Timeout 太短，或发送失败未处理                    |
| 多电机反馈串台     | 未同时校验 Master ID 与`D[0]` 低 4 bit，或存在重复路由键                   |
| 位置方向相反       | 先确认电机参数/方向设置；不要在驱动层偷偷取负号，方向应显式配置              |
| stop 后仍有力      | 检查是否真的发送`...FD` 到当前模式对应控制 ID，并核对反馈状态是否回到 0    |
| 清错后马上复发     | 不要反复清错掩盖硬件问题；根据 8～E 状态处理电压、电流、温度、通信或负载根因 |

状态码：`8` 超压、`9` 欠压、`A` 过流、`B` MOS 过温、`C` 线圈过温、`D` 通讯丢失、`E` 过载。

## 9. 第二阶段扩展方式

MIT 接口和硬件链路验证后，再开放：

1. `setPositionVelocity(dev, p, v_limit)`：构建 `0x100+ID`、8 字节小端 float 帧。
2. `setVelocity(dev, v)`：构建 `0x200+ID`、4 字节小端 float 帧。
3. Bus 的 arm 中，位置-速度模式用最新反馈位置作为初始目标，速度上限先设为 0 或极小值，避免使能后突然回零。
4. 纯速度模式用 `v=0` 作为安全初始命令。
5. 是否扩展通用 `motor::Api`，等至少两种厂商驱动都需要统一位置/速度语义后再决定；不要让 DM 私有控制模式绑架通用接口。

“保存零点”会改变电机持久状态，必须提供独立、命名醒目的维护接口，并要求设备已失能；不要把它放入自动启动流程。

## 10. 最终勾选清单

- [ ] 调试助手已读取并记录型号、电压版本、控制模式、CAN ID、Master ID、PMAX/VMAX/TMAX。
- [ ] `dm_protocol` 对标准帧、DLC、端序、12/16 bit 拼接和所有范围做校验。
- [ ] 语义零 MIT 测试向量通过，代码中没有把全零线帧当零输出。
- [ ] `dm_motor` 接入通用 torque/feedback/state，但没有伪装成 current 接口。
- [ ] 公共温度取 rotor，原始反馈保留 MOS 和 rotor 两项。
- [ ] DM Bus 对相同 Master ID 只建一个过滤器，并按 motor ID 分发。
- [ ] attach 检查反馈路由冲突和最终控制帧 ID 冲突。
- [ ] 所有命令先缓存再由单一控制线程 flush；命令/反馈都有超时。
- [ ] 任一准备或发送失败都会使整条 DM Bus 尝试失能并锁存 Fault。
- [ ] stop 使用 `...FD`，recover 使用 `...FB` 后再失能，并等待正常反馈确认。
- [ ] 第一版只允许设备树 `control-mode="mit"`。
- [ ] 样例先运行 0 Nm，再短时运行 0.02～0.05 Nm，并有超速/过温/超时停机。
- [ ] 机构已悬空、CAN 终端与共地正确、急停可用、电压版本核对无误。
- [ ] 构建、协议测试和实机测试结果均已记录。
