# GM6020 速度环曲线诊断与调参指南

## 1. 结论先行（已根据第二张平台曲线更新）

第二张截图已经是 `5 rad/s` 恒速平台。光标处十个通道可互相校验：

```text
目标/参考速度       5.000 rad/s
瞬时反馈速度        2.199 rad/s
瞬时误差            2.801 rad/s
P 输出               0.0700 A
最终电流             0.0700 A
饱和标志             0
反馈年龄             约 1 ms
```

`2.801 × 0.025 ≈ 0.0700`，说明截图与源码中的纯 P 参数完全吻合，通道没有错位。

现在看到的不是普通的细小测速毛边，而是规则、幅度很大的“掉速—加速—冲过目标—再掉速”循环。速度尖峰远大于整数 RPM 的单格 `0.10472 rad/s`，因此量化只能触发或加重振荡，不能单独解释整条曲线。饱和标志为零且反馈年龄正常，也基本排除了软件电流饱和和 CAN 超时。

高概率原因是低速极限环：纯 P 必须靠较大的持续误差产生克服摩擦的电流；速度接近或冲过目标时，P 电流迅速减小甚至反向，电机又因摩擦快速掉速，于是重复加速。直驱、低惯量和未滤波 RPM 会进一步加重这个循环。

下一轮分两次做：

**试验 A：确认 P 对极限环的影响**

```cpp
.kp = 0.015f,
.ki = 0.0f,
.kd = 0.0f,
.derivative_tau_s = 0.0f,
.integral_min = 0.0f,
.integral_max = 0.0f,
.deadband = 0.0f,
```

继续保持 `5 rad/s` 平台，只比较振荡的频率、峰峰值以及电流是否反向。平均速度变低是纯 P 降低后的正常现象，不要因此立刻把 Kp 加回去。

**试验 B：如果 A 的尖峰明显变缓，再用小积分提供维持电流**

```cpp
.kp = 0.015f,
.ki = 0.02f,
.kd = 0.0f,
.derivative_tau_s = 0.0f,
.integral_min = -0.08f,
.integral_max =  0.08f,
.deadband = 0.0f,
```

`Ki=0.02` 在误差约 `2.8 rad/s` 时每秒累加约 `0.056 A`，大约一秒便能建立当前曲线所暗示的 `0.05～0.07 A` 维持电流。预期是 I 输出逐渐承担主要维持电流，P 输出及速度误差下降，周期尖峰明显收敛。

若试验 A 把 Kp 降到 `0.015` 后，速度仍以近似相同的周期和幅度跳变，则暂时不要加积分，应先做本文后述的固定电流对照试验，以区分 PID 极限环和电机/反馈自身的周期性问题。

上述数值仍是诊断起点，不是最终参数。

### 当前 PI 已压到约 ±1 rad/s 后怎么办

最新源码已经变为：

```cpp
.kp = 0.02f,
.ki = 0.02f,
.kd = 0.000002f,
.derivative_tau_s = 0.0f,
.integral_min = -0.08f,
.integral_max =  0.08f,
```

如果实测误差已从上一轮的大幅极限环降到约 `±1 rad/s`，说明小积分方向是有效的，但尚未到硬件极限。整数 RPM 的分辨率约为 `0.105 rad/s`，`±1 rad/s` 仍约等于 `±9.5 RPM`，有继续优化空间。

不要用 `deadband=1.0` 消掉图上的误差。死区只是让控制器在这个误差范围内不动作，会接受而不是消除 `±1 rad/s`。

按振荡快慢选择下一步：

| 现象 | 首先调整 | 下一档 | 原因 |
|---|---|---|---|
| 高频、密集抖动，I 曲线基本平稳 | 降 Kp | `0.020 → 0.015 → 0.012` | 降低 P 对 RPM 波动的即时反应 |
| 慢速来回摆，I 曲线也明显往复 | 降 Ki | `0.020 → 0.010 → 0.005` | 积分追得太快，形成低频摆动 |
| 速度机械上平滑，但 VOFA 反馈毛 | 滤速度反馈 | `tau=0.010～0.020 s` | 处理反馈估计噪声，不盲目降带宽 |
| I 长期贴在 `+0.08` 或 `-0.08 A` | 查维持电流/前馈 | 不先加 Ki | 积分已经没有更多控制余量 |
| 固定电流也有同周期速度脉动 | 查电机和机械 | 暂停 PID 调参 | 波动源不在外部速度 PID |

结合前一张曲线的高频特征，推荐下一次只改：

```cpp
.kp = 0.015f,
.ki = 0.02f,
.kd = 0.0f,
.derivative_tau_s = 0.0f,
.integral_min = -0.08f,
.integral_max =  0.08f,
```

保持其他量不变，观察 3～5 秒平台段。重点单独放大显示通道 2（速度）、3（误差）、5（I 输出）和 7（总电流）：

- 若高频误差明显缩小而平均误差仍接近零，保留 `Kp=0.015`；
- 若振荡频率降低但幅度不降，并且 I 输出同步摆动，把 `Ki` 再降到 `0.01`；
- 若机械感觉平滑、只有通道 2 高频跳，应加入真实的速度反馈低通，而不是继续降低 PI；
- 若误差振荡时总电流几乎不动，说明控制器不是主要激励源，应做固定电流对照。

当前 `Kd=0.000002` 对一个 RPM、一个控制周期的速度跳变只产生约 `0.000042 A`，几乎没有阻尼效果。把它增大又会迅速放大测速噪声，所以本阶段直接归零；不要试图用 D 消除这类高频误差。

如果 PI 和 `10～20 ms` 速度低通后仍只能到 `±1 rad/s`，下一步不是无限加增益，而是改测速来源：利用连续编码器位置在 `20～40 ms` 滑动窗口内求速度，或使用 PLL/观测器。按 8192 tick/圈估算，20 ms 差分的理论速度量化约为：

```text
(2π / 8192) / 0.020 ≈ 0.038 rad/s
```

它比电机报文的整数 RPM 台阶更细，但会引入窗口延迟，仍需重新确认相位裕度。只有固定电流下原始速度读数也周期跳变、且确认机械转速实际平滑时，才值得进入这一步。

## 2. 本次只读检查到的真实状态

目标文件：`samples/motor/dji_speed_control/src/main.cpp`。

检查时工作区已有用户修改，本文不覆盖它们。第二张截图对应的最新源码为：

```text
控制周期                 5 ms，名义 200 Hz
速度平台                 +5 rad/s
请求函数                 500 ms 后持续返回 +5 rad/s
软件电流限幅             ±0.8 A
设备树电流边界           ±3 A
Kp                       0.025 A/(rad/s)
Ki                       0 A/(rad/s·s)
Kd                       0 A/(rad/s²)
积分项限幅               -0.3 A 到 +0.3 A，但 Ki=0
误差死区                 0
D 项滤波时间常数         0 s
速度反馈                 整数 RPM 直接换算，未低通
```

这里有三个很容易误判的点：

- 当前 `ki = 0`，所以即使积分范围写成 `±0.3 A`，积分输出仍然为零，当前是纯 P 控制器。
- `kDeadbandRadS = 0.20f` 虽然已经声明，但配置实际写的是 `.deadband = 0.0f`，所以死区没有启用。
- 当前 `Kd=0`，所以 D 与 D 滤波均未参与。

源码注释中还残留着 `0.25 A`、`0.30 A` 和负方向等旧描述，与当前 `0.8 A`、设备树 `3 A` 以及实际正弦请求不完全一致。调参时以代码和实测为准，不要以旧注释判断当前限制。

## 3. 控制数据流

```text
+5 rad/s 平台请求
       │
       ▼
斜率限制器（±100 rad/s²） ─────► velocity_reference
       │
       ▼
速度误差 = reference - 整数 RPM 换算值
       │
       ▼
P / I / 测量微分 D + 前馈
       │
       ▼
PID 输出限幅 ±0.8 A
       │
       ▼
GM6020 电流指令 ─► 电机 ─► CAN 整数 RPM 反馈
```

当前平台在 `500 ms` 时从零请求 `5 rad/s`，斜率限制器以 `100 rad/s²` 在约 `50 ms` 内把实际参考升到 `5 rad/s`。截图显示的是之后的稳定平台段，因此斜率限制器不是周期尖峰的原因。

## 4. 先把 VOFA 通道认对

右侧旧名字可以不管，但通道顺序不能弄错。`main.cpp` 实际发送的十个通道依次是：

| 通道 | 实际含义 | 单位 | 调参用途 |
|---:|---|---|---|
| 0 | 原始速度请求 `request` | rad/s | 看测试波形 |
| 1 | 斜率限制后的速度参考 | rad/s | PID 真正目标 |
| 2 | 电机反馈速度 | rad/s | 看跟踪和噪声 |
| 3 | 速度误差 | rad/s | 看稳态误差 |
| 4 | P 输出 | A | 看 P 是否放大反馈波动 |
| 5 | I 输出 | A | 当前应恒为零 |
| 6 | 前馈输出 | A | 当前应恒为零 |
| 7 | 最终电流命令 | A | 判断是否高频换向 |
| 8 | PID 饱和标志 | 0/1 | 判断是否撞限幅 |
| 9 | 反馈年龄 | ms | 判断是否反复使用旧反馈 |

每轮至少同时显示通道 `1、2、3、4、7、8、9`。只显示目标和速度，无法区分“测速显示毛刺”和“闭环真的在高频打电流”。

重点观察：

- 蓝线跳变是否接近 `0.1047 rad/s` 的整数倍；若是，量化影响明确存在。
- 蓝线变粗时，通道 7 是否同步快速正负切换；若同步，闭环确实在追逐噪声或发生极限环。
- 通道 8 是否频繁为 1；若是，不要再加增益，先降低目标加速度或确认限流是否合理。
- 通道 9 是否周期性接近或超过 `20 ms`；若是，应先解决 CAN 反馈或控制调度问题。
- 通道 5 在当前配置下若不是严格为零，说明烧录固件与正在查看的源码不一致。

## 5. 推荐的实机调参顺序

### 第一步：把正弦临时改成恒速平台

在 `requestedVelocityForTime()` 处手工改成：

```cpp
float requestedVelocityForTime(std::int64_t elapsed_ms)
{
    if (elapsed_ms < 1000) return 0.0f;
    if (elapsed_ms < 4000) return 5.0f;
    if (elapsed_ms < 5000) return 0.0f;
    if (elapsed_ms < 8000) return -5.0f;
    return 0.0f;
}
```

同时把运行时间临时改成约 `9000 ms`，避免一次异常试验持续 300 秒。初调阶段建议把软件限流先降到实机能够安全急停的值，例如 `0.3 A`；设备树边界也应采用经过硬件确认的较低值，而不是把 `3 A` 当作调速手段。

为什么用平台：平台上可以分别测出上升时间、稳态误差、维持电流和持续振荡；正弦曲线上这些现象会叠在一起。

### 第二步：只调 Kp

保持 `Ki=0、Kd=0、I 限幅为 0、死区为 0、前馈全为 0`。

1. 从 `Kp=0.015` 开始，分别看 `+5` 和 `-5 rad/s` 平台。
2. 若响应平稳但太慢，依次尝试 `0.020、0.025、0.030`，每次只改一个值。
3. 找到刚开始出现持续等幅速度波纹、啸叫或电流高频换向的 `Kp_crit`。
4. 最终先取 `0.5～0.7 × Kp_crit`，给温度、供电、负载和安装刚度变化留稳定裕度。

第一张截图里的 `Kp=0.03` 已经值得怀疑是在临界值附近或以上；第二张平台曲线进一步确认 `Kp=0.025` 时仍存在大幅规则极限环。下一档直接试 `0.015`，不要在 `0.020～0.025` 之间微调浪费试验次数。

本步骤允许存在稳态速度误差。纯 P 必须保留一定误差才能产生克服摩擦和负载的电流，这不是此阶段的失败判据。

通过条件：

- 平台段没有持续高频厚带或机械啸叫；
- 电流不会持续快速正负切换；
- 正、反方向行为接近；
- 饱和标志不会长时间为 1；
- 反馈年龄正常。

### 第二步半：必要时做固定电流对照

只有当 `Kp=0.015` 的纯 P 曲线仍以近似相同周期和幅度跳变时才做此试验。可临时利用本样例现有的前馈 `k_bias` 产生固定电流，不必改驱动：

```cpp
// feedback 内保持合法的 dt 和输出限幅配置，只把三个增益置零
.kp = 0.0f,
.ki = 0.0f,
.kd = 0.0f,

// feedforward 内只启用固定偏置
.k_bias = 0.05f,
.k_static = 0.0f,
.k_velocity = 0.0f,
.k_acceleration = 0.0f,
```

本次平台曲线证明正 P 输出能产生正速度，所以对当前接线可先从 `k_bias=+0.03 A` 开始，再试 `+0.05 A`；若实机方向或接线改变，必须重新用更小电流确认符号。把运行时间先缩短到 `3～5 s`，并准备物理断电。

判断方法：

- 通道 7 电流命令应是一条严格水平线；
- 固定电流下速度变平滑：问题主要来自闭环参数，继续试小 P、小 I；
- 固定电流下仍有相同周期尖峰：先检查机械卡滞、GM6020 电流环固件、供电、电机相线/编码器和原始 `speed_rpm`，不要再调 PID；
- 固定电流平滑但速度较低并不异常，本试验只判断周期尖峰是否仍存在。

试验结束必须把 `k_bias` 恢复为零。固定偏置不会因目标回零而自动消失，不能带着它进入正常闭环或长期运行。

### 第三步：再加入小积分

保持第二步选定的 Kp。建议从以下配置起步：

```cpp
.ki = 0.01f,
.integral_min = -0.06f,
.integral_max =  0.06f,
```

然后只按 `0.01 → 0.02 → 0.04` 增大 Ki，直到平台稳态误差在要求时间内消除。积分限幅应根据平台段的实测维持电流确定，通常先取其绝对值的 `1.2～1.5` 倍；`±0.06 A` 只是空载小电流场景的起点。

第二张曲线对应的 `Ki=0` 没有积分作用。不要直接跳到此前试过的 `Ki=0.3`：误差为 `1 rad/s` 时，它一秒就会累加 `0.3 A`，对当前约 `0.05～0.07 A` 的维持电流量级明显过激。

出现以下现象时，把 Ki 回退 `30%～50%`：

- 速度开始低频上下摆；
- 换向后长时间拖尾；
- 回到零给定后仍继续转；
- I 输出长期贴住积分限幅。

### 第四步：有需要才加死区

若 P、PI 已稳定，但目标附近仍有约一两个 RPM 台阶引起的轻微电流来回跳，可尝试：

```cpp
.deadband = 0.10f,  // 先约一个 RPM 台阶
```

再视情况试 `0.15` 或现有常量 `kDeadbandRadS` 的 `0.20`。死区会把带内的 P 误差和积分增量都置零，设得过大会直接降低跟踪精度，并可能形成较宽的速度游走带，所以不能用大死区掩盖明显振荡。

### 第五步：若厚带仍明显，再对速度反馈低通

现有 `derivative_tau_s` 只滤 D 使用的测量变化率，不会滤送入 P 和 I 的速度。若确认原始 RPM 抖动明显，应在速度 PID 前增加一阶低通，初值可用 `tau = 0.02 s`：

```text
alpha = dt / (tau + dt)
filtered_velocity += alpha * (raw_velocity - filtered_velocity)
```

名义 `dt=0.005 s、tau=0.02 s` 时 `alpha=0.2`，截止频率约 `8 Hz`；相对于当前 `0.159 Hz` 的正弦目标，附加相位滞后很小。

手工实现位置：

1. 在 `VelocityController` 增加 `filtered_velocity_rad_s` 和初始化状态；
2. 在 `resetController()` 用首次反馈初始化滤波值，避免上电跳变；
3. 在 `calculateVelocityCurrent()` 内先计算下一滤波值，再把它填入 PID 的 `measurement`；
4. 仅当整次控制计算成功时提交下一滤波状态，保持当前“失败不修改状态”的事务语义；
5. 调试阶段同时发送原始速度和滤波速度，确认没有用滤波掩盖 CAN 异常。

先试 `tau=0.01、0.02、0.03 s`。滤得越重，速度曲线越平滑，但闭环相位裕度会变差；如果出现低频摇摆或制动变迟，应减小 `tau` 或重新降低 Kp。

### 第六步：最后才考虑 D 和前馈

速度 D 先保持零。整数 RPM 单格在 5 ms 内跳变对应约：

```text
0.10472 / 0.005 ≈ 20.94 rad/s²
```

因此较大的 D 会直接制造电流尖峰。只有存在清楚的机械共振、且 P/PI 与速度低通无法取得足够阻尼时，才尝试很小的 D；届时滤波时间常数从 `0.02～0.05 s` 起，而不是当前的 `0.001 s`。

恢复正弦后，如果平台测试很好、但正弦在过零附近仍明显滞后，优先识别速度/加速度前馈，而不是继续硬加 P、I：

- `k_velocity` 补偿与速度近似成比例的损耗；
- `k_acceleration` 补偿惯量所需转矩；
- 每次只启用一个前馈项，并保留总输出限幅。

前馈系数应来自多个恒速平台和加减速试验的实测电流，不要凭截图猜数值。

## 6. 恢复正弦后的验收

平台 PI 调稳后，再恢复 `18·sin(t)`，按以下顺序提高测试难度：

```text
±5 rad/s 正弦 → ±10 rad/s 正弦 → ±18 rad/s 正弦
```

每档保存相同时间尺度、相同纵轴范围的截图和 CSV。至少记录：

| 试验 | Kp | Ki | I 限幅 | deadband | 低通 tau | 峰值误差 | 平台/均方误差 | 峰值电流 | 饱和占比 | 是否抖动 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 01 | | | | | | | | | | |

正弦验收时重点分开看：

- 峰值附近误差大：更可能与摩擦、速度前馈或积分有关；
- 过零附近误差大：这里加速度最大，更可能与带宽、电流限制或加速度前馈有关；
- 全程都是高频厚带：更可能是 P 太高、反馈噪声、机械共振或控制时序问题；
- 只在电流饱和时跟不上：增益不是根因，先处理电流能力、目标加速度或负载。

## 7. 接口和边界行为

本样例实际使用 `lib/control/pid.c`，不要与 `include/drivers/pid/pid.h` 的另一套 PID 混用。

```c
int control_feedforward_pid_step(
    control_feedforward_pid_state *state,
    const control_feedforward_pid_config *config,
    const control_feedforward_pid_input *input,
    control_feedforward_pid_result *result);
```

- 成功返回 `0`，写入下一状态和结果；参数、有限数或时间步非法时返回负 errno。
- `dt_s` 必须在配置的 `0.001～0.020 s` 内；否则样例会走安全停机。
- D 是对测量值求导并取负号，因此设定值突变不会直接产生 derivative kick，但测速台阶会进入 D。
- 积分限幅的单位已经是安培，是乘过 Ki 后的 I 输出，不是误差积分原值。
- 抗饱和逻辑只阻止继续推向同方向饱和的积分；从饱和恢复方向的积分仍可更新。
- CAN 回调在驱动回调上下文中通过 spinlock 更新反馈，主循环读取快照；不要移除新鲜度检查或在回调里做浮点控制。
- 任何读反馈、计算、设电流或总线发送错误都会进入 `stopAfterFailure()`；调参时不要绕过该安全路径。

## 8. 构建、烧录与安全上电

本文没有执行构建、烧录或电机试验。手工修改后可在 west 工作区执行：

```bash
west build -p -b rm_typec samples/motor/dji_speed_control \
  -d ../build/dji_speed_control_rm_typec
west flash -d ../build/dji_speed_control_rm_typec
```

若实际板卡不是 `rm_typec`，必须替换板名，并确认 VOFA 使用的 `usart6` 与物理串口一致。

每次上电：

1. 先断开动力或悬空负载，检查 CAN、ID、方向、减速比和机械限位；
2. 把运行时间、速度与软硬件限流降到当前试验所需的最小值；
3. 准备物理断电手段后再给电机上电；
4. 每次只改一个参数并保存曲线；
5. 一旦持续啸叫、剧烈抖动、异常升温、长期饱和或反馈超时，立即物理断电。

## 9. 最终检查清单

- [ ] VOFA 通道已按源码顺序重新命名。
- [ ] 当前固件版本与源码参数一致，I 通道行为与积分限幅一致。
- [ ] 已用 `±5 rad/s` 平台重新寻找 Kp，而不是只看正弦。
- [ ] Kp 已留出至少约 30% 的稳定裕度。
- [ ] Ki 从小值开启，积分限幅为对称且来自实测维持电流。
- [ ] D 在 P/PI 未稳定前保持为零。
- [ ] 已同时检查最终电流、饱和标志和反馈年龄。
- [ ] 正反方向、不同速度和实际负载均已复测。
- [ ] 最终 `±18 rad/s` 正弦没有持续高频厚带或长期电流饱和。
- [ ] 源码旧注释已由操作者在确认实机参数后同步修正。
