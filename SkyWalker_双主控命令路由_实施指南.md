# SkyWalker 双主控命令路由实施指南

## 1. 结论与现状

可以支持云台板、底盘板分工，无需推翻原架构。原《SkyWalker_机器人应用层与消息架构_施工规格.md》第 10 节已经把 RobotCommand 分成 chassis、gimbal、shooter；第 12 节要求 CommandManager 不读 UART、不直接看 CAN。这正好允许在其输出之后增加 CommandRouter（命令路由器），把子命令交给本板执行器或板间通信出口。

原规格第 35、36 节主要描述单板上的 zbus 和线程，尚未提供跨板传输、远端反馈、重启与断链语义。因此是“结构适合扩展”，不是“当前已经具备双板实现”。本文引用的 robotics 文件多数属于原规格中的待建模块；当前 application/src/main.c 与 application/CMakeLists.txt 为空。

暂假设遥控和主要决策输入位于云台板，发射机构也由云台板执行。若实车接线不同，可以移动决策入口或调整静态路由表；仍应明确每个子系统唯一的最终命令来源。未确认实际板型、UART 引脚、电气接口、波特率及旧协议。当前源码搜索找到 lib/vofa/vofa.c 的串口回调，没有找到旧板间通信实现，不能认定 VOFA 就是以前的板间协议。

## 2. 部署与数据流

```text
云台板
遥控 / 视觉 / 决策输入
  → SafetyManager + CommandManager
  → RobotCommand
  → CommandRouter
      ├─ gimbal → 本地命令通道 → 云台控制 → 本地 CAN
      ├─ shooter → 本地命令 / 事件通道 → 发射机构
      └─ chassis → 最新待发命令 → BoardLink 编码 → UART
                                                       ↓
底盘板                                      UART → BoardLink 解码
                                                       ↓
                                        校验来源 / 顺序 / 有效性
                                                       ↓
                                        本地安全门 → 底盘命令通道
                                                       ↓
                                        舵轮解算与本地电机闭环
                                                       ↓
                                              底盘板自己的 CAN

底盘状态 / 故障 / 就绪状态 → BoardLink → 云台板反馈通道 → 决策层
```

CommandManager 负责选择“执行什么”，Router 负责“交给哪里”，BoardLink 负责“如何跨板传输”。底盘接收后不再次进入向远端转发的通用路由入口，避免回环。

两块板各有自己的 zbus 实例；同名 channel 不会自动跨板。BoardLink 在发送侧取消息，在接收侧校验后重新发布本地消息。

跨板传输底盘速度 vx、vy、wz、模式和使能等目标即可。四个舵轮的解算、八个电机的反馈与控制周期留在底盘板，不需要逐个电机通过云台板中转。命令更新频率和本地电机闭环频率独立设计。

双板是否足够，需要测量 CPU 周期与每条 CAN 的负载。八个电机并不直接等于单板算力不足；如果底盘的八个电机依然挂同一条 CAN，这条总线的负载不会因为云台移到另一块板而自动降低。必要时还需在底盘板的不同 CAN 接口之间分配电机。高总线负载与 CRC、位错误等通信错误应分别排查，不能只凭电机数量归因。

## 3. 手工修改顺序与接口边界

以下均为拟新增或拟调整的位置，本文没有创建这些业务文件。

| 顺序 | 文件 | 手工修改内容与自检点 |
|---|---|---|
| 1 | include/robotics/messages/command.hpp | 沿用三个子命令；明确 chassis 速度均为底盘坐标系，m/s、rad/s。若需要云台坐标系操纵，使用新鲜的云台相对底盘 yaw 做变换后发送，不能拿云台绝对航向直接代替相对角。 |
| 2 | include/robotics/command/command_router.hpp 与 lib/robotics/command/command_router.cpp | 增加纯逻辑静态路由；不依赖 UART、zbus；核对每个子命令只有一个执行板。 |
| 3 | include/communication/board_link_codec.hpp 与 lib/communication/board_link_codec.cpp | 显式编码、流式拆包、校验与长度限制；输入不完整时保留残片，完整且合法才输出消息。 |
| 4 | application/src/board_link_task.cpp | UART 缓冲管理、发送调度、接收队列、会话管理和本地接收时间；不可在串口回调内运行控制算法。 |
| 5 | application/src/message_bus.*、command_task.cpp | 增加本板子命令通道、远端反馈通道；仲裁完成后调用路由器。 |
| 6 | application/src/chassis_task.cpp、gimbal_task.cpp | 只消费本板子命令，每个控制周期检查本地安全条件及命令有效期。 |
| 7 | application/CMakeLists.txt、prj.conf、板级 overlay | 建立应用入口，明确云台/底盘角色，选择无冲突的 UART、CAN 和设备；只启用实际挂载的执行器。 |

可采用下面的接口形状；类型需要按原规格补齐，属于设计伪代码：

```cpp
// command_router.hpp：纯函数，无内部队列、无 I/O
// 返回本地子命令集合和远端子命令集合；配置在初始化时检查。
RouteResult route(const RobotCommand &command, const BoardRoutes &routes);

// board_link_codec.hpp：由通信线程独占，不可同时从 ISR 和线程调用。
// 成功返回写入字节数；空间不足 -ENOSPC；字段非法 -EINVAL。
int encode(const WireCommand &command, uint8_t *dst, size_t capacity);
// 返回 NeedMore / FrameReady / Invalid；FrameReady 才更新 out。
// 必须支持连续多帧；调用方循环取出所有完整帧。
DecodeResult feed(ByteSpan bytes, WireMessage &out);

// 接收入口：时间参数必须是接收板自己的单调时间。
// Accepted 更新命令槽及接收时间；其余结果不刷新有效期。
AcceptResult accept(const WireCommand &command, uint64_t local_rx_ms);
```

AcceptResult 至少区分 Accepted、WrongDestination、WrongSource、OldSequence、BadSession、BadValue、Disarmed。路由配置错误应在初始化时报错并保持禁用，不能隐式让两个板同时驱动同一子系统。

应用层行为示意：

```text
云台 command_task:
    resolved = CommandManager.step(inputs)
    routed = route(resolved, routes)
    publish_local(routed.local)
    replace_pending_remote(routed.remote)  // 连续目标只保留最新值

底盘 board_link_task:
    从接收队列取字节，逐帧解码
    校验版本、长度、CRC、来源、目的、会话、序号和字段范围
    若可接受：保存命令和本地接收时间，发布本地快照

底盘 chassis_task 每个控制周期:
    原子读取命令快照及其接收时间
    若未就绪、未授权使能、命令超时或本地执行器故障：执行预定停机策略
    否则：限幅 → 舵轮解算 → 本地电机控制
```

命令和时间戳必须作为一个快照交接，不能出现“旧命令配新时间”。UART 回调只交接字节和缓冲区所有权；通信线程负责解析。发送缓冲在驱动完成或中止前不能重用。队列溢出必须计数并触发重同步，不能默默拼接缺字节的数据。

可参考 lib/vofa/vofa.c 的 UART 回调与发送生命周期、samples/motor/dji_position_control/src/main.cpp 的设备绑定方式；不能照搬 VOFA 的文本分行解析作为二进制协议，也不能忽略异步接收事件中的 offset、分片和多帧问题。具体 UART 缓冲操作需对照实际 Zephyr 版本的驱动接口确认。

## 4. 协议与故障语义

第一版只跨板发送连续 ChassisCommand 和底盘状态，先不做任意消息转发框架。

建议帧字段依次为：帧头、协议版本、消息类型、源板、目的板、会话标识、递增序号、负载长度、负载、CRC。实现前双方固定各字段宽度、大小端、CRC 参数和覆盖范围，并形成一份共享协议定义；本文不声称兼容旧协议。

- 不直接 memcpy C++ 结构体到 UART：结构体填充、枚举宽度和浮点表示必须明确。可以选定小端、固定宽整数以及明确规定的 IEEE 754 binary32；解析后仍要拒绝 NaN、Inf 和越界值。
- 帧长必须有上限；损坏帧重新寻找帧头。CRC 只能检测传输损坏，来源字段用于部署校验，不是密码学身份认证。
- 32 位序号按模运算判断新旧：差值 d = new - old，以无符号 32 位计算，只有 0 < d < 2^31 才视为更新；重复帧不刷新命令有效期。
- 两板启动时间不一致，不能直接用接收板时钟减去发送板 uptime。第一版用本地接收时间判断断流；同时禁止发送端积压旧目标，命令生产停止后不能把旧目标重新编号当成新命令发送。若需要严格的生成到执行时限，另加时钟同步或接收端签发的短期有效授权。
- 心跳只代表通信端在线，不能刷新运动命令有效期。示例可从命令 100 Hz、断流 100 ms 开始评估，最终阈值需依据实测延迟与停车距离确定。
- 重启或断链恢复后先进入禁用；通过新会话握手、双方就绪和明确使能再恢复，清除旧命令及事件。会话标识必须能够区分重启，不能每次固定从同一个值开始。
- 底盘本地安全门独立检查电机反馈超时、执行器故障、功率限制及急停条件。云台板输出使能不等于底盘必须执行；本地可以拒绝。
- 停机动作按机械系统定义：减速归零、保持转向、撤销驱动并不是同一种行为。通信超时必须能在执行板独立触发，不能依赖另一板再发“停止”。
- 若裁判功率数据等安全约束只在云台板读取，应转发必要状态及其有效性；底盘数据失效时使用预定保守策略。
- 反馈至少包含本地就绪状态、实际速度、故障、最后接受命令序号及数据有效性；云台板同样对反馈断流计时。
- 日后跨板发送单发、清故障等事件，使用独立事件 ID、确认和去重；连续速度采用最新值覆盖。断链期间事件不自动重放，不能承诺跨重启“恰好执行一次”。

## 5. 带宽估算与验证步骤

以假设的 64 字节完整帧、100 Hz、UART 8N1 为例：每字节占 10 位，每个方向需要 64 × 100 × 10 = 64000 bit/s。若选择 115200 bit/s，该方向占用约 55.6%，单帧串行化时间约 5.56 ms。这里的帧长和波特率只是计算示例，并非已确认配置。分别核算双向流量；半双工链路还需合计双方流量与切换开销。

CAN 负载按每条物理总线分别计算：各类帧实际线缆位数（含协议开销和填充）× 频率之和 / 总线速率。需要统计电机反馈、分组控制帧、其他节点以及重发。先测周期耗时、队列溢出、发送等待、总线错误计数，再决定 CAN 分配与频率。

手工完成后按顺序检查：

1. 两板只上逻辑电源、电机不使能；确认电平标准、引脚与链路接线，再观察双方会话建立和状态反馈。
2. 发送已知 vx、vy、wz，检查底盘解码后的单位、符号、坐标系和序号；确认云台命令仍在云台板本地执行。
3. 检查分片、连续帧、损坏帧、重复帧、发送任务停止但心跳继续、串口断开及单板重启；预期非法消息不更新命令，旧目标不复活，断流后执行板进入预定安全状态。
4. 再使电机低限幅运行，机械可靠支撑并保留物理断电手段；确认方向后逐步增加负载，测本地控制周期及各 CAN 负载。

待应用工程和板角色配置实际建成后，可在仓库根目录使用独立构建目录。例如两块板都为 dm_mc02 时：

```bash
# 先在应用中完成并选择对应角色；当前空应用不能直接完成此构建。
west build -b dm_mc02/stm32h723xx -d build/gimbal application
# 将应用配置选择为底盘角色后：
west build -b dm_mc02/stm32h723xx -d build/chassis application
```

实际板型不同时替换 board 参数，并保存各自角色配置以避免烧错板。本次未运行构建、测试或硬件验证。

## 6. 完成清单

- [ ] 每个子系统执行板和唯一命令来源已确定。
- [ ] CommandManager、路由和串口协议职责分离。
- [ ] 坐标系、单位、字节布局及版本双方一致。
- [ ] 本地控制不等待 UART，发送不堆积旧目标。
- [ ] 两板独立断流保护，重启后须重新建立会话与使能。
- [ ] 双向反馈、错误计数、链路预算和 CAN 预算已验证。
- [ ] 已确认旧板间协议是否需要兼容及真实硬件接线。

本次只新增本指南，原施工规格和业务源码均未修改。
