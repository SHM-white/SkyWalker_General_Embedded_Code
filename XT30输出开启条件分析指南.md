# DM-MC02 XT30 输出开启条件分析指南

## 1. 结论先行

当前板级设备树已经完整声明了两路 XT30 的开关控制，**若应用在运行时调用 Zephyr regulator API，则不需要再写 overlay**。

| 输出 | 设备树标签 | 使能 GPIO | 有效电平 | 固件启动后的默认状态 |
|---|---|---|---|---|
| XT30_1 | `power1` | `PC14` | 高电平 | 关闭 |
| XT30_2 | `power2` | `PC13` | 高电平 | 关闭 |

真正的软件开启条件是：对应 regulator 设备初始化成功，并且第一次成功调用 `regulator_enable()`，使控制脚输出有效高电平。除此以外，板卡输入电源、硬件保护和外部功率通路也必须正常；设备树只能控制使能脚，不能保证接口物理上一定测到电压。

## 2. 设备树逐项解释

文件：`boards/damiao/dm_mc02/dm_mc02.dts`

```dts
power1: xt30_1_regulator {
	compatible = "regulator-fixed";
	regulator-name = "XT30_1";
	enable-gpios = <&gpioc 14 GPIO_ACTIVE_HIGH>;
	regulator-boot-off;
};
```

- `power1:` 是节点标签，应用代码可以用 `DT_NODELABEL(power1)` 找到它。
- `compatible = "regulator-fixed"` 表示它由 Zephyr 的固定稳压器驱动管理。这里的“稳压器”是软件抽象；从这段 DTS 能确定的是 GPIO 开关控制，不能确定 XT30 的实际输出电压。
- `enable-gpios = <&gpioc 14 GPIO_ACTIVE_HIGH>` 表示 PC14 为高时启用 XT30_1，低时关闭。
- `regulator-boot-off` 要求启动时保持关闭。固定稳压器驱动初始化时会把该 GPIO 配置成 inactive，因此本例为低电平。
- 节点没有写 `status`。Zephyr 规定 `status` 缺省等价于启用，因而两个 regulator 节点都能创建设备实例。

XT30_2 完全相同，只是节点标签为 `power2`，控制脚为 PC13。

板级 `dm_mc02_defconfig` 已有：

```conf
CONFIG_GPIO=y
CONFIG_REGULATOR=y
```

由于设备树中存在启用的 `regulator-fixed` 节点，`CONFIG_REGULATOR_FIXED` 会在 GPIO 条件满足时默认选中。因此通常不需要在应用 `prj.conf` 中重复配置。

## 3. 从启动到输出的控制链

```text
系统启动
  -> fixed-regulator 驱动初始化
  -> PC14、PC13 配置为输出 inactive（低）
  -> 两路 XT30 默认关闭
  -> 应用取得 power1 / power2 对应的 device
  -> device_is_ready() 成功
  -> 第一次 regulator_enable()
  -> 对应 GPIO 置为 active（高）
  -> 板上功率开关导通
  -> 在输入供电和硬件保护均正常时，XT30 才有物理输出
```

`regulator_enable()` 使用引用计数。第一次调用把计数从 0 变为 1，并真正拉高 GPIO；之后重复调用只增加计数。每次成功的 enable 都应有一次对应的 `regulator_disable()`，计数回到 0 时才真正拉低 GPIO。

不要一边使用 regulator API、一边用 GPIO API直接操作 PC13/PC14，否则驱动内部状态和引脚真实状态可能不一致。

## 4. 推荐方案：应用运行时开启，不写 overlay

在需要使用 XT30 的应用源文件中加入：

```c
#include <errno.h>
#include <zephyr/device.h>
#include <zephyr/devicetree.h>
#include <zephyr/drivers/regulator.h>

#define XT30_1_NODE DT_NODELABEL(power1)
#define XT30_2_NODE DT_NODELABEL(power2)

static const struct device *const xt30_1 = DEVICE_DT_GET(XT30_1_NODE);
static const struct device *const xt30_2 = DEVICE_DT_GET(XT30_2_NODE);

static int xt30_enable(const struct device *dev)
{
	if (!device_is_ready(dev)) {
		return -ENODEV;
	}

	return regulator_enable(dev);
}
```

使用示例：

```c
int ret = xt30_enable(xt30_1);
if (ret < 0) {
	/* 记录错误并保持负载关闭。 */
	return ret;
}
```

不再使用时：

```c
ret = regulator_disable(xt30_1);
if (ret < 0) {
	/* 记录关闭失败。 */
}
```

建议先完成系统自检，再开启功率输出；发生通信超时、过温、急停或其他安全故障时，释放所有成功取得的 enable 引用并关闭输出。

### 自检点

1. 编译后检查最终 `zephyr.dts`，应包含 `XT30_1`、`XT30_2`、PC14、PC13 和 `regulator-boot-off`。
2. 上电但尚未调用 `regulator_enable()` 时，测量 PC14/PC13 应为低。
3. 先不接电机等大功率负载，调用 enable 后测量对应控制脚应为高。
4. 确认接口电压与极性正确后再接负载。
5. 调用一次 enable 后调用一次 disable，控制脚应回到低；若曾 enable 多次，必须匹配相同次数的 disable。

## 5. 什么时候才需要 overlay

### 场景 A：某个应用要求固件初始化阶段自动开启

这时可以在该应用的 `app.overlay` 中覆盖默认策略：

```dts
&power1 {
	/delete-property/ regulator-boot-off;
	regulator-boot-on;
};
```

若两路都要自动开启，再对 `&power2` 做同样处理。必须先删除 `regulator-boot-off`，不要让 boot-on 和 boot-off 同时存在。

注意：`regulator-boot-on` 初始化时会建立一个引用计数；之后一次 `regulator_disable()` 就可以关闭它。如果要求软件永远不能关闭，应使用 `regulator-always-on`，但不建议对外接大功率负载轻率采用该策略。

### 场景 B：应用要改引脚、极性或禁用一路

这类应用级硬件差异也适合 overlay。例如禁用 XT30_2：

```dts
&power2 {
	status = "disabled";
};
```

### 不需要 overlay 的场景

- 运行到某个业务状态后开启 XT30；
- 根据按键、通信命令或自检结果开关 XT30；
- 故障时由应用关闭 XT30。

这些都直接调用 regulator API 即可。

## 6. 构建和硬件验证建议

请在对应应用目录按项目原有方式执行 pristine 构建，以避免旧设备树缓存影响结果。例如：

```sh
west build -p always -b dm_mc02 <应用目录>
```

构建后查看：

```sh
grep -n -A8 -B2 'xt30_[12]_regulator' build/zephyr/zephyr.dts
grep -E '^CONFIG_(GPIO|REGULATOR|REGULATOR_FIXED)=' build/zephyr/.config
```

预期至少看到 `CONFIG_GPIO=y`、`CONFIG_REGULATOR=y`、`CONFIG_REGULATOR_FIXED=y`。

## 7. 风险与未验证假设

- DTS 没有声明 `regulator-min-microvolt` 或 `regulator-max-microvolt`，所以无法仅靠设备树判断 XT30 的输出电压。`regulator-name` 只是名字，不代表电压值。
- DTS 只能证明 PC13/PC14 的软件极性，无法证明功率 MOS、保险、输入电源、欠压/过流保护等硬件条件。最终必须结合 MC02 原理图或硬件手册并实测。
- MCU 复位后到 Zephyr GPIO 驱动初始化前，引脚通常不是由这段 regulator 驱动主动控制；这段极短时间内的物理电平取决于芯片复位态和板上上下拉，需查原理图确认。
- XT30 可能承载较大电流。首次验证应断开电机负载，使用限流电源和万用表，确认极性和电压后再接负载。

## 8. 最终勾选清单

- [ ] 确认要控制的是 XT30_1（PC14）还是 XT30_2（PC13）。
- [ ] 确认应用采用运行时 API，还是明确需要启动即开启。
- [ ] 运行时控制时不新增 overlay。
- [ ] 调用 `device_is_ready()` 后再调用 `regulator_enable()`。
- [ ] 检查每个成功 enable 都有匹配的 disable。
- [ ] 不用 GPIO API直接争用 PC13/PC14。
- [ ] 空载、限流条件下验证控制脚和 XT30 输出。
- [ ] 从原理图/手册确认实际输出电压、最大电流和保护条件。
