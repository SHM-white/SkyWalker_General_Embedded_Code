# DM-J4310 绿灯约一秒后失能排查指南

## 1. 结论先行

当前现象可以确定两件事：

1. 电机曾经变为绿灯只能证明 XT30 已经给电机供电，不能单凭灯色证明本程序发送的 Enable 已被执行。后续日志显示程序一直停留在 Disabled 反馈握手阶段，`arm()` 根本没有执行；短暂绿灯应视为电机上电自检现象，除非总线上还存在另一台控制器。
2. 在能够收到 Disabled 反馈并进入控制循环后，任何一次检查失败都会调用 `stopAfterFailure()` 发送失能命令，然后从 `main()` 返回；程序没有恢复、重试或重新使能逻辑。

DM-J4310 V1.1 手册规定：绿灯常亮是使能状态，红灯常亮是失能状态，红灯闪烁才是故障状态。因此应先确认后半段究竟是稳定常亮，还是频率较高、肉眼容易看成常亮的闪烁。仓库内手册见 `docs/DM-J4310-2EC_V1.1_User_Manual.pdf`。

最新日志中的 `status_ret=-61` 是 `-ENODATA`：驱动从启动到超时没有接收过一帧符合配置的反馈。当前已经排除“使能后运行期保护触发”，因为程序尚未执行到 `arm()` 和控制循环。需要区分：总线上完全没有返回帧，还是返回帧因 Master ID、电机 ID 或帧格式不匹配而被驱动过滤。

当前最可能的两类原因是：

- 调试助手中的 Master ID、电机 ID、控制模式或 CAN/CAN FD 模式与 overlay 不一致；
- CAN1 的物理接收链路异常，主控能发送但收不到电机反馈。

`kCommandTorqueNm` 当前已被改为 `0.01f`。这不等于“零扭矩”，尽管数值很小；首次排查建议恢复为 `0.0f`，避免引入额外机械变量。

## 2. 实际执行链

```text
MC02 启动
  -> CONFIG_BOARD_DM_MC02 成立
  -> regulator_enable(XT30_1)
  -> CAN1 启动，安装 Master ID 0x000 接收过滤器
  -> 发送 Disable（0xFD）
  -> 收到 Disabled 反馈
  -> 发送 Enable（0xFC）和中性 MIT 帧
  -> 电机绿灯
  -> 每 5 ms：读反馈 -> 检查状态/速度/温度 -> 设置扭矩 -> 发送控制帧
       |
       +-- 任一步失败 -> stopAfterFailure()
                         -> 发送 Disable（0xFD）
                         -> 电机红灯
                         -> main() 返回，不再重试
```

关键源码位置：

- XT30_1 开启：`samples/motor/dm_mit_control/src/main.cpp:93`
- 初始失能、等待反馈和使能：`samples/motor/dm_mit_control/src/main.cpp:130`
- 所有运行期保护出口：`samples/motor/dm_mit_control/src/main.cpp:150`
- `stopAfterFailure()` 发送失能：`samples/motor/dm_mit_control/src/main.cpp:52`
- `Bus::flush()` 任一准备或发送错误都会进入 Fault 并失能：`drivers/motor/dm/dm_bus.cpp:265`
- 反馈超过 Kconfig 时限会被判为 Offline：`drivers/motor/dm/dm_motor.cpp:21`

## 3. 当前会导致红灯的所有条件

### 3.1 反馈状态不是 Ready

```cpp
if (skywalker::motor::getState(motor) != skywalker::motor::State::Ready) {
    return stopAfterFailure(-EHOSTDOWN);
}
```

当前 `CONFIG_SKYWALKER_DM_FEEDBACK_TIMEOUT_MS=50`。只要连续 50 ms 没有收到一帧能通过以下条件的反馈，状态就会变成 Offline：

- 标准 CAN 帧；
- DLC 为 8；
- 帧 ID 等于 `master-id`，当前为 `0x000`；
- 数据首字节低四位的电机 ID 等于 `motor-id`，当前为 `1`。

如果反馈携带 8～E 的故障码，驱动会锁存 Fault，状态同样不再是 Ready。

### 3.2 样例保护阈值触发

当前任一条件成立都会返回 `-ERANGE`（通常打印为 `cause=-34`）：

- `abs(velocity_rad_s) > 1.0`；
- 转子温度不低于 60°C；
- MOS 温度不低于 60°C。

`1 rad/s` 约等于输出轴 `9.55 rpm`，对手动拨动或上电瞬态来说偏严格。不要直接删除保护；先把实际速度和温度打印出来，确认是哪一项触发。

### 3.3 `setTorque()` 或 `flush()` 失败

- `-EHOSTDOWN`（通常为 `-112`）：反馈超过 50 ms、收到故障状态，或故障已锁存。
- `-EACCES`（通常为 `-13`）：总线或电机已不在 Armed 状态。
- `-ESTALE`（通常为 `-116`）：命令时间戳超过当前 20 ms 命令时限，或生命周期 epoch 不一致。
- CAN 驱动的负错误码：控制帧未能进入发送路径，例如总线错误或发送超时。

当前代码在 `flush()` 失败后没有打印 `flush_report.preparation_error`，而 `stopAfterFailure()` 打印的是随后执行 stop 的报告。这会丢失第一次失败的关键上下文，是目前最需要补齐的诊断信息。

## 4. 先不改代码的检查

### 4.1 获取正确串口的日志

DM-MC02 的 Zephyr console 是 `USART10`，引脚为 PE2/PE3，115200 8N1；VOFA 遥测使用的是另一路 `USART1`。不要只盯着 VOFA 串口等待 `LOG_ERR`。

重点记录最后出现的消息：

- `MIT zero-torque loop started`：证明初始化、初始反馈和 `arm()` 都成功。
- `motor disabled: cause=...`：第一判断依据。
- `Arm failed: ...`：若出现，说明观察到的短暂绿灯发生在 arm 过程，但软件没有真正进入循环。

请保存从复位开始到红灯后的完整日志，不要只截最后一行。

### 4.2 查看 VOFA 第 0 通道

代码按以下顺序发送六个通道：

1. `status`
2. 位置 rad
3. 速度 rad/s
4. 扭矩 N·m
5. MOS 温度 °C
6. 转子温度 °C

第 0 通道含义：

| 数值      | 含义         |
| --------- | ------------ |
| 0         | Disabled     |
| 1         | Enabled      |
| 8         | 过压         |
| 9         | 欠压         |
| 10（0xA） | 过流         |
| 11（0xB） | MOS 过温     |
| 12（0xC） | 电机线圈过温 |
| 13（0xD） | 通讯丢失     |
| 14（0xE） | 过载         |
| 15（0xF） | 未识别状态   |

记录绿灯结束前最后一帧的六个数。如果最后状态仍为 1、随后数据停止，优先怀疑反馈超时或 CAN 发送错误；如果状态已经变成 8～14，则按对应电机故障处理。

### 4.3 核对硬件与调试助手参数

当前 overlay 固定为：

```text
CAN 控制器：CAN1
CAN 波特率：1 Mbps
Motor ID：1
Master ID：0
控制模式：MIT
P_MAX：12.5 rad
V_MAX：45 rad/s
T_MAX：18 N·m
```

调试助手中的 Motor ID、Master ID、控制模式、P_MAX、V_MAX、T_MAX 必须逐项一致。还要确认 CAN_H/CAN_L 没有接反、控制板与电机共地、总线末端有合适的 120 Ω 终端，并在电机使能期间测量 XT30 电压是否明显下跌。

## 5. 推荐的第一轮源码诊断修改

只修改 `samples/motor/dm_mit_control/src/main.cpp`。目的不是绕过安全保护，而是在失能前保存真正原因。

### 5.1 首次排查使用真实零扭矩

把：

```cpp
constexpr float kCommandTorqueNm = 0.01f;
```

改为：

```cpp
constexpr float kCommandTorqueNm = 0.0f;
```

自检：启动日志中的文字 `MIT zero-torque loop started` 此时才与实际命令一致。

### 5.2 为状态和保护条件补充现场数据

在控制循环读取完 `feedback` 和 `raw` 后，先保存状态：

```cpp
const auto state = skywalker::motor::getState(motor);
const std::int64_t now_ms = k_uptime_get();
const std::int64_t feedback_age_ms =
    now_ms >= static_cast<std::int64_t>(raw.timestamp_ms)
        ? now_ms - static_cast<std::int64_t>(raw.timestamp_ms)
        : -1;
```

把状态失败分支改成以下形式：

```cpp
if (state != skywalker::motor::State::Ready) {
    LOG_ERR("state failure: state=%u drive=%u age_ms=%lld",
            static_cast<unsigned int>(state),
            static_cast<unsigned int>(raw.status),
            feedback_age_ms);
    return stopAfterFailure(-EHOSTDOWN);
}
```

把速度/温度保护分支改成：

```cpp
if (std::fabs(feedback.velocity_rad_s) > kSpeedCutoffRadS ||
    feedback.temperature_c >= kTemperatureCutoffC ||
    static_cast<float>(raw.mos_temperature_c) >= kTemperatureCutoffC) {
    LOG_ERR("limit failure: drive=%u vel_mrad_s=%d mos=%u rotor=%u age_ms=%lld",
            static_cast<unsigned int>(raw.status),
            static_cast<int>(feedback.velocity_rad_s * 1000.0f),
            static_cast<unsigned int>(raw.mos_temperature_c),
            static_cast<unsigned int>(raw.rotor_temperature_c),
            feedback_age_ms);
    return stopAfterFailure(-ERANGE);
}
```

这里把速度乘 1000 后作为整数打印，是为了避免嵌入式日志的浮点格式支持问题。`age_ms` 表示反馈帧年龄：若大于 50，就直接证明是反馈新鲜度触发。

### 5.3 保留第一次 `flush()` 失败报告

把原来的：

```cpp
ret = dm_bus.flush(flush_report);
if (ret < 0) {
    return stopAfterFailure(ret);
}
```

改为：

```cpp
ret = dm_bus.flush(flush_report);
if (ret < 0) {
    LOG_ERR("flush failure: ret=%d prep=%d tx=%d sent=%u expected=%u failed_id=%u",
            ret,
            flush_report.preparation_error,
            flush_report.tx_error,
            flush_report.frames_sent,
            flush_report.frames_expected,
            flush_report.failed_motor_id);
    return stopAfterFailure(ret);
}
```

自检：再次出现红灯时，`state failure`、`limit failure` 或 `flush failure` 三类日志中必须至少出现一类。

## 6. 根据诊断结果采取措施

### A. `drive=1`，但 `age_ms > 50`

说明电机仍是使能状态或最后反馈是使能状态，但主控超过 50 ms 没收到新反馈。

处理顺序：

1. 用 CAN 分析仪确认电机是否持续从 Master ID `0x000` 返回 DLC=8 的标准帧。
2. 若总线上有反馈但主控收不到，检查 FDCAN1 接线、过滤器 ID 和中断/错误状态。
3. 若电机反馈本来就低于 20 Hz，再按实测周期把 `CONFIG_SKYWALKER_DM_FEEDBACK_TIMEOUT_MS` 调到至少“最大反馈间隔的 2～3 倍”。不要仅为了掩盖丢帧任意增大。

### B. `drive=8～14`

这是电机上报的真实故障。不要自动循环重启。

- 8/9：测量电机端实际电压，特别是绿灯期间是否下陷；确认电机是 24 V 版还是 48 V 版。
- 10：断开机械负载，恢复零扭矩，检查是否卡死和供电限流。
- 11/12：等待冷却并检查温度设置、散热和温度读数。
- 13：检查控制帧是否持续到达、调试助手内的通讯丢失保护时间。
- 14：卸载机械负载，检查堵转和参数。

先排除故障原因，再使用驱动已有的 `Bus::recover()` 流程发送 ClearError；不要不断发送 Enable。

### C. `cause=-34` 且 `drive=0/1`

这是样例自身的速度或温度保护，不是电机故障。依据新增日志确定具体数值。如果仅为手动拨动触发，可在机械悬空、零扭矩条件下根据实验目标调整速度阈值，但应保留合理上限。

### D. `flush failure` 中 `tx < 0`

这是 CAN 发送路径错误。读取 Zephyr CAN 状态和错误计数，检查 ACK、终端电阻及总线是否进入 error-passive/bus-off。不能把 `can_send()` 成功简单等同于电机执行成功；还需要持续反馈作为闭环确认。

### E. `flush failure` 中 `prep=-116`

说明 20 ms 的命令时限被突破。测量 `setTorque()` 到 `flush()` 的耗时和线程是否被高优先级任务长时间抢占。当前两者紧邻，正常情况下不应发生，因此不要直接先放宽到很大数值。

## 7. 后续健壮化建议

诊断完成后再考虑以下结构改造：

- XT30 开启后等待电机完成上电自检，再启动握手；绿灯已经出现说明这不是本次第一嫌疑，但能提高冷启动稳定性。
- `waitForDisabled()` 增加总超时，并在超时前周期性重发 Disable；当前无限等待不利于定位。
- 运行期故障不要直接静默退出。失能后进入明确的 Safe/Fault 状态循环，持续低频输出最后错误信息，等待人工复位或受控 `recover()`。
- 对 `arm()` 后第一帧 Enabled 反馈设置确认超时，不能只凭 CAN 发送 API 返回成功就认定电机已稳定使能。
- 把驱动状态、反馈年龄、CAN 错误计数作为低频诊断遥测，控制帧仍保持固定周期。

## 8. 建议构建与硬件验证

完成上述手工修改后，在 west 工作区根目录执行：

```sh
source .venv/bin/activate
west build -p always -b dm_mc02 skywalker_code/samples/motor/dm_mit_control
```

硬件验证顺序：

1. 电机输出轴悬空或可靠卸载，急停/断电手段可立即触及。
2. 使用限流 24 V 电源；先确认所持电机不是 48 V 版本。
3. 连接 USART10 日志和 USART1 VOFA，再给主控上电。
4. 保存从复位到红灯后的完整日志和 VOFA 最后一帧。
5. 先依据日志分类，再修改超时或保护阈值；一次只改变一个变量。

预期成功现象：绿灯保持常亮，VOFA 第 0 通道持续为 1，反馈年龄始终明显小于 50 ms，控制循环持续发送，不出现任何 failure 日志。

## 9. 最终检查清单

- [ ] 已确认后半段是红灯常亮还是红灯闪烁。
- [ ] 已从 USART10 获取完整日志。
- [ ] 已记录 `motor disabled: cause` 的数值。
- [ ] 已记录 VOFA 最后一帧 status、速度和两个温度。
- [ ] Motor ID=1、Master ID=0、MIT 模式与调试助手一致。
- [ ] 已把测试扭矩恢复为 0.0 N·m。
- [ ] 已补充 state/limit/flush 三处诊断日志。
- [ ] 未在原因不明时删除保护或自动反复使能。
- [ ] 已在限流、卸载、可快速断电条件下复测。
