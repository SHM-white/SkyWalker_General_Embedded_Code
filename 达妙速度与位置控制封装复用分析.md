# 达妙速度与位置控制封装复用分析

> 本文记录重构前的分析。后续已实现 `VelocityMotor` / `PositionMotor` 并移除旧 C++ wrapper，
> 当前用法见 [统一速度与位置控制](samples/motor/MOTOR_CONTROL.md)。

## 结论与范围

现有 `VelocityController`、`PositionController` 可以复用，且两个 DM MIT 示例已经在复用。它们是保存算法状态的 C++ wrapper，不负责绑定电机、启动 CAN、使能、周期调度或发送命令。

本次只读分析当前仓库实现，未修改业务代码，未编译或上板。这里的 wrapper 指 `include/control/velocity_controller.hpp` 和 `include/control/position_controller.hpp`。以下协议模式描述均指代码实现；实际电机持久化模式、量程和反馈回绕行为尚未通过硬件验证。

## 当前四条路径

| 路径 | 现有示例 | 控制接口 | 是否复用控制器 wrapper |
| --- | --- | --- | --- |
| MIT 软件速度环 | `samples/motor/dm_mit_velocity_control/src/main.cpp` | `VelocityController::step()` → `motor::setTorque()` | 已复用 |
| MIT 软件位置、速度串级环 | `samples/motor/dm_mit_position_control/src/main.cpp` | `PositionController::step()` → `motor::setTorque()` | 已复用 |
| 电机内置速度环 | `samples/motor/dm_velocity_control/src/main.cpp` | `dm::setVelocity()` | 不需要软件速度环 |
| 电机内置位置、速度环 | `samples/motor/dm_position_control/src/main.cpp` | `dm::setPositionVelocity()` | 不需要软件串级环 |

```text
MIT 软件速度：速度目标 → VelocityController → 力矩缓存 → DM Bus.flush → 电机
MIT 软件位置：连续位置目标 → PositionController → 力矩缓存 → DM Bus.flush → 电机
                              └ 位置 PID → 软件速度环
原生速度：速度目标 → dm::setVelocity → 命令缓存 → DM Bus.flush → 电机内部闭环
原生位置：位置目标 + 速度上限 → dm::setPositionVelocity → 命令缓存 → DM Bus.flush
反馈：CAN 回调 → DM 驱动反馈缓存 → 应用检查状态/有效性 → 控制器或监控
```

`setTorque()` 最终构造只有 `torque_ff_nm` 非零的 MIT 命令，其余 MIT 字段初始化为零。因此当前软件闭环路径没有同时配置 MIT 的位置、速度增益。

不要把 `effort_command` 传给 `dm::setVelocity()` 或 `dm::setPositionVelocity()`：前者输出的是执行器力矩/电流，后两者需要速度/位置。若将来需要“软件位置环 + 电机内置速度环”，可以单独复用底层位置 PID，并把输出定义为 rad/s；现有 `PositionController` 已经包含软件速度环，不能直接拿最终输出替代速度目标。

## wrapper 已经封装了什么

### 速度控制

依据 `lib/control/motor_velocity.c`，包含目标速度检查、参考速度斜率限制、测量低通滤波、软死区、前馈 + PID、最终 effort 限幅。wrapper 保存配置、状态、初始化标志和最后一次成功输出。

### 位置控制

依据 `lib/control/motor_position.c`，位置 PID 输出速度参考，再调用同一个 C 速度控制内核。它接受连续目标和连续位置，不包含角度解包、单圈最短路径、保存零点或轨迹生成。

注意位置积分当前在 C 内核中固定为 `freeze_integrator = true`。因此配置了位置 `ki` 并不代表正常累积位置积分；这不是 DM 示例单独的配置，而是所有使用该控制器的共同语义。

### 关键接口约定

| 接口 | 参数与返回 | 状态和边界 |
| --- | --- | --- |
| `validate() const` | 无参数；成功 0，否则负 errno | 检查配置，不初始化控制器 |
| `VelocityController::reset(float measured_velocity_rad_s, float initial_reference_rad_s = 0)` | rad/s；成功 0 | 清理控制历史、初始化测量与参考速度；成功后清空最后输出 |
| `PositionController::reset(float measured_position_rad, float measured_velocity_rad_s)` | rad、rad/s；成功 0 | 初始化位置和内层速度状态；成功后清空最后输出 |
| `VelocityController::step(float requested_velocity_rad_s, float measured_velocity_rad_s, float dt_s, Output &output)` | rad/s、秒；成功 0 | 未 reset 返回 `-EACCES`；非有限输入一般 `-EINVAL`；目标越界等返回 `-ERANGE` |
| `PositionController::step(float target_position_rad, float measured_position_rad, float measured_velocity_rad_s, float dt_s, Output &output)` | 连续 rad、rad/s、秒；成功 0 | 未 reset 返回 `-EACCES`；不自动归一化角度；位置输出必须匹配内层允许速度范围 |

两者的 C 内核先计算局部状态，成功才提交；step 失败不覆盖原状态和输出，wrapper 也只在成功时更新 `lastOutput()`。失败后不能继续把上一拍输出当成当前有效命令发送，应走应用故障停机路径。

wrapper 无锁，应由一个控制线程持有和调用；不要在 CAN 接收回调中运行控制循环。驱动用自旋锁保护反馈/命令缓存，但这不代表多个线程可以随意并发操作整个 Bus 生命周期。

## 复用边界与当前缺口

### 1. 算法通用，输出单位仍由应用决定

DJI 示例调用 `motor::setCurrent(motor, output.effort_command)`，单位 A；DM MIT 示例调用 `motor::setTorque(...)`，单位 N·m。`include/control/motor_velocity.h` 和 `motor_position.h` 已明确记录这一区别。

因此可复用类和计算流程，不能直接照搬 DJI 的 PID、前馈系数和 effort 限幅。当前类型系统都是 `float`，不会替调用方检查 A 与 N·m 是否混用。对于 DM，控制器力矩上限还必须不超过 `Descriptor::torque_limit_nm`。

### 2. 达妙反馈尚未满足通用连续位置约定

`include/drivers/motor/motor.hpp` 声明 `Feedback::position_rad` 是连续输出轴位置、首帧为零；但 `drivers/motor/dm/dm_motor.cpp` 的 `acceptFeedback()` 直接赋值 `decoded.position_rad`，没有首帧偏置或跨量程累计。

DM MIT 位置示例在应用里通过下面的逻辑弥补连续性：

```cpp
delta = std::remainder(current_raw_position - previous_raw_position, 2 * PMAX);
continuous_position += delta;
```

例如 PMAX=12.5，反馈从 +12.49 到 -12.49，原始差是 -24.98，按周期 25 解包得到约 +0.02 rad。这里的周期是 `2*PMAX`，不能直接换成 `2π`。该算法要求实际反馈在边界回绕，并且相邻有效样本的真实位移绝对值小于 PMAX；若饱和或丢帧后运动过大，无法据此恢复正确转角。

当前 DM 没有声明 `FeedbackAbsolutePosition` 能力，不能直接复用 DJI 示例依赖 `absolute_position_rad` 的固定零点路径。现有 DM 示例还包含“按时间正向增加 90°”“选择前方零点”“相对目标坐标计算”等业务策略，这些不应塞进通用 PID wrapper。

短期复用应保留 DM 的反馈适配。长期若要做到更换电机而应用不变，需统一驱动位置语义，同时单独保留原生位置命令需要的保存零点坐标，不能简单把驱动所有位置都改成首帧归零。

### 3. 原生速度、位置接口尚未进入通用 motor API

通用 `motor::Api` 只有 current、torque、feedback、state。DM 的原生速度/位置能力由 `dm::setVelocity()`、`dm::setPositionVelocity()` 暴露。

这些 setter 做参数检查和命令暂存，不立即发 CAN。有效参数下，模式不匹配返回 `-ENOTSUP`，未 armed 返回 `-EACCES`，故障或反馈过期返回 `-EHOSTDOWN`；非有限原生输入返回 `-EINVAL`，超范围返回 `-ERANGE`。位置接口接受零速度上限，当前位置示例主动要求其大于零。

成功调用 setter 后仍要 `Bus::flush()`。模式来自配置，不会因为调用另一个 setter 就自动切换电机内部持久化模式。现在只为了 DM 单一应用复用，没有必要扩展全项目 `motor::Api`；若以后需要统一原生控制后端，再补能力位和接口契约。

### 4. 会话与保护可以复用，但仍是示例层

`samples/motor/dm_common/` 已抽出 `Session`、`prepare`、`arm`、`readSafeFeedback`、`flush`、`stopAfterFailure`。它集中处理总线启动、供电、握手、状态和速度/温度保护。

MIT 示例的 Session/VOFA 使用静态生命周期；原生两个示例仍使用局部对象。CAN 过滤器保存 Bus 的 `this`，Bus 未提供析构注销过滤器路径，所以复用成可能返回的函数时，必须保证回调对象长期存活，或另行设计完整清理，不能盲目照搬局部生命周期。

命令超时检查在发送路径发生；线程完全停止执行时，软件无法继续主动发送失能。控制 wrapper 本身也没有硬件看门狗功能。

## 如需进一步封装，按这个最小顺序动手

本节是后续手工实施建议，本次分析不要求立即改源码。

1. **先复用现有 MIT 示例。** 从对应 `main.cpp` 提取自身目标生成和配置，保留现有 controller 类、DM Session、反馈检查和错误处理。自检：每周期只有一条 effort 输出路径，所有负返回值均停止正常发送。
2. **先明确位置坐标，再抽取位置适配。** 以 DM MIT 位置示例的首次初始化和 `remainder` 累计代码为依据，将“反馈连续化”和“目标路径策略”分开；可以先放在应用自己的辅助代码中。自检：确认目标/测量共用零点，跨反馈量程不跳变，重新初始化时不沿用旧累计状态。未验证反馈回绕前不要把这段升级成全局驱动保证。
3. **只有多处应用出现重复时再提取执行层。** 新层负责每周期读反馈、step、缓存命令及错误传播；保持 `include/control/*controller.hpp` 和 `lib/control/motor_*.c` 不依赖 DM/CAN。多电机共用 Bus 时，在同一周期完成各电机命令更新后集中 flush，避免每个轴各自管理总线启动和发送。

在应用控制循环内可直接参考如下顺序；这是伪代码，配置、周期计算和反馈变量沿用现有示例：

```cpp
// 启动：prepare → validate → 读取有效反馈 → reset → arm。
// 每拍：使用真实 dt_s；当前 MIT 示例超过 20 ms 即停机。
ret = readSafeFeedback(session, speed_cutoff, temperature_cutoff, feedback, raw);
if (ret < 0) return stopAfterFailure(session, ret);

ret = controller.step(target_speed, feedback.velocity_rad_s, dt_s, output);
// 位置路径改为连续目标、连续测量、实测速度、dt_s、output。
if (ret < 0) return stopAfterFailure(session, ret);
ret = skywalker::motor::setTorque(session.motor, output.effort_command);
if (ret < 0) return stopAfterFailure(session, ret);
ret = flush(session);
if (ret < 0) return stopAfterFailure(session, ret);
```

## 手工验证与未验证项

当前只做静态源码分析，没有运行生成工作区产物的命令。后续修改后可在已配置 Zephyr 的环境手工执行：

```sh
west build -b dm_mc02/stm32h723xx -d /tmp/skywalker-dm-velocity-review samples/motor/dm_mit_velocity_control
west build -b dm_mc02/stm32h723xx -d /tmp/skywalker-dm-position-review samples/motor/dm_mit_position_control
```

上板前先确认电机持久化模式、CAN/主机 ID、波特率及 P/V/T 量程与 overlay 一致。固定机身、输出轴悬空并准备物理断电；首次使用保守力矩上限。文档建议初次为 0.05 N·m，但当前两个 MIT 示例常量实际是 0.5 N·m，应以源码为准并在首次试验前手工调整。

共享启动顺序是 CAN 初始化/注册过滤器 → MC02 开启 XT30_1 → 等待 1.5 s → 反馈握手 → 初始化控制器 → arm。原生示例没有软件控制器初始化步骤。不要在自动启动中调用保存零点。

预期 MIT 速度示例跟踪 2 rad/s；MIT 位置示例每 6 s 正向增加 90°，显示单圈角度但内部目标保留圈数。跨量程位置跳变先查反馈回绕和坐标适配；`-ENOTSUP` 查模式/能力；`-EACCES` 查 reset 或 arm 阶段；`-EHOSTDOWN` 查反馈和驱动状态；`-ESTALE` 查命令缓存更新时间。

- [ ] 明确采用软件 MIT 闭环还是原生闭环。
- [ ] 保留现有控制器 wrapper，按 DM 的 N·m 重新配置系数和限幅。
- [ ] 位置目标与反馈坐标一致，并验证固件跨量程行为。
- [ ] 回调对象生命周期覆盖 CAN/UART 使用期间。
- [ ] 每周期更新命令并 flush，失败后停止正常控制。
- [ ] 若后续手工改动，完成对应构建和受控上板验证。
